{
  "name": "whatsapp-multi-session-bot",
  "version": "2.0.0",
  "description": "Modern WhatsApp Multi-Session Bot with Next.js Frontend",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web & npm run dev --workspace=apps/bot",
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:bot": "npm run dev --workspace=apps/bot",
    "build": "npm run build --workspaces",
    "start": "npm run start --workspace=apps/web & npm run start --workspace=apps/bot"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
